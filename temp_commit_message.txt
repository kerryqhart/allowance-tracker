🧹 Clean up all compiler warnings (51 warnings fixed)

- Remove unused imports from backend files (error, warn, FixedOffset, NaiveDate, etc.)
- Fix unused variables by prefixing with underscore
- Mark intentional dead code with #[allow(dead_code)]
- Remove legacy Tauri directories and files (src-tauri/, frontend/, src/)
- Update workspace configuration to reflect current structure
- Application now builds and runs with zero warnings

Changes include:
- Cleaned up log imports (removed unused error, warn)
- Cleaned up chrono imports (removed unused FixedOffset, NaiveDate)
- Removed unused async_trait, Context, Arc, PathBuf imports
- Prefixed unused parameters in stub implementations
- Marked repository fields and helper methods as intentionally unused
- Updated workspace Cargo.toml to only include active crates 