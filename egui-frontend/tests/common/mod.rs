// egui-frontend/tests/common/mod.rs
//! Shared test helpers for `egui-frontend`'s integration tests. A companion
//! file to `two_machine.rs` — not itself part of Task 14's listed
//! deliverables, but required for `mod common; use common::two_machine::...`
//! to resolve at all (the classic `tests/common/mod.rs` shared-helpers
//! pattern).

pub mod two_machine;
