refactor(goal): use domain-level transaction listing

GoalService now queries TransactionService via the new `list_transactions_domain` and internal `TransactionListQuery`, removing its dependency on shared DTOs.

This pushes the DTO-to-domain boundary further outward in the transactions migration. 