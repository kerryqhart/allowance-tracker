# SDD ledger — plan: docs/superpowers/plans/2026-09-07-lgs-desktop-sync.md

Spec: docs/superpowers/specs/2026-08-29-lgs-desktop-sync-design.md (read; binding authority)
Worktree: /Users/kerryhart/Code/allowance-tracker/.worktrees/lgs-desktop-sync
Branch: lgs-desktop-sync
Base: fed60f8

## Pre-flight conflict scan

### Pairwise — tasks sharing a file or interface

| Tasks | Produces → consumes | Finding |
|---|---|---|
| 2 → 3 | `Money::{from_cents,cents,render}`, `FromStr`, serde | clean |
| 2 → 5 | `Money::render`, `parse::<Money>()` | clean |
| 4 → 5 | `TxRow`, `TxType` | **F1** — ownership of `row.rs` stated two ways |
| 4 → 7 | `recompute_running_balances` | clean |
| 4 → 8 | `recompute_running_balances`, `validate` | clean |
| 4 → 15 | `TxRow`, `Sided`, `Provenance` | clean |
| 5 → 8 | `parse_transactions`, `render_transactions` | clean |
| 5 → 15 | `render_transactions` (ApplyMerge) | clean |
| 3 → 6 | `generate_id(amount: Money, ..)` | **F2** — T3 already changes the signature; T6 changes only entropy |
| 7 → 8 | `merge(Option<&[TxRow]>, &Sided, &Sided) -> MergeOutcome` | **F3** — re-key is not a fixed point |
| 9 → 10,11,12,18 | `SyncPaths` | **F4** — interface block omits `home`, code has it |
| 10 → 12 | `DaemonState` variants | clean |
| 10 → 15,18,19 | `StatusReport`, `ProjectReport` | clean |
| 11 → 12 | both edit `bootstrap.rs`, T11 creates / T12 appends | clean (ordered) |
| 13 → 15,19 | `ensure_lgs_remote`, `fetch_lgs`, `push_lgs` | clean |
| 13 → 17 | `child_sync.rs` created T15, modified T17 | clean (ordered) |
| 15 vs 16 | both edit `app_coordinator.rs`, different match arms | clean |
| 18 → 19 | both edit `migration_lgs.rs` | clean (ordered) |
| 9 → 18 | `is_cloud_synced` | clean |

### Self-consistency — each task's tests against its own code

| Task | Finding |
|---|---|
| 1 | clean |
| 2 | clean — `render` verified for negative and sub-dollar; `FromStr` verified for `"5.5"` → 550 |
| 3 | clean — serde emits `-5.5` / `27.25` as numbers, matching the wire assertions |
| 4 | **F1** |
| 5 | **F5** — test named `..._is_preserved_not_derived` asserts an error |
| 6 | clean (given **F2**) |
| 7 | clean — all 10 tests traced against the match arms, including both tie-break directions |
| 8 | **F6** — `dedup_by` without a prior sort leaves duplicate ids |
| 9 | **F4** |
| 10 | clean — `#[serde(default)]` covers a fixture without `adoptable` |
| 11 | clean |
| 12 | clean |
| 13 | **F7** — `GitManager` gains a `clock` field; `new()` must initialise it |
| 14 | **F8** — Step 2 contains an ellipsis instead of the registration code |
| 15 | **F9** — test targets `classify`/`Cycle`; code provides only `cycle`/`CycleOutcome` |
| 16–21 | clean — test helpers are implementer-authored, which is acceptable at this granularity |

## Rulings

Ruling: gitignore `.superpowers/` — the skill assumes the workspace is ignored and it was not; without it the ledger and review packages would be committed. Cost if wrong: none, it is scratch. (commit 8e1f0d2)

Ruling (F3, load-bearing): the add/add re-key suffix derives from the row's intrinsic **content**, not from provenance, and `merge` dedupes by final id before recomputing. A provenance-derived suffix is not a fixed point — re-merging sees the same collision on the original id and re-keys again with a fresh suffix forever, so Task 8's `fixed_point` property would fail and the two machines would never stop merging. Plan text amended in Task 7. Cost if wrong: the fixed-point property fails in Task 8 and the merge needs rework before Phase 3.

Ruling (F3b): `content_suffix` is an explicit FNV-1a rather than `DefaultHasher`. Rust does not guarantee `DefaultHasher` output is stable across compiler versions, and this value becomes part of a transaction id that two separately-built machines must agree on. Cost if wrong: none — FNV-1a is fully specified.

Ruling (F1): Task 4 owns `allowance-core/src/row.rs` and defines `TxRow`, `TxType`, `Provenance`, `Sided`. Tasks 5, 7, 15 consume and never redefine. Plan text amended. Cost if wrong: a duplicate type definition caught at compile time.

Ruling (F4): `SyncPaths` carries `home: PathBuf`. The guard compares against `home/Library/Mobile Documents` and `home/Documents`; reading `dirs::home_dir()` internally is exactly what would make the most important safety rule in the spec untestable. Plan text amended. Cost if wrong: none.

Ruling (F6): Task 8's `sided_strategy` sorts by id before `dedup_by`, which only removes *consecutive* duplicates. Without it the generator emits duplicate ids, the merge's id index silently drops one, and symmetry appears to fail for a reason that is purely a generator artifact. Plan text amended. Cost if wrong: spurious proptest failures that waste a fix round.

Ruling (F9): Task 15 produces a pure `classify(ours, auth, base) -> Cycle` alongside `cycle() -> CycleOutcome`; `cycle()` calls `classify` after fetching. Keeps the branch decision testable without a repository. Plan text amended. Cost if wrong: the Step 2 test cannot compile and the implementer improvises a name.

Ruling (F5): renamed the Task 5 test to `an_unknown_type_is_refused_not_derived` to match what it asserts. Cost if wrong: none.

Ruling (F2): Task 3 changes `generate_id`'s signature to take `Money`; Task 6 changes only the suffix entropy. Carried into Task 6's dispatch so its implementer is not surprised the signature is already migrated. Cost if wrong: minor confusion, no rework.

Ruling (F7): Task 13's `GitManager::new()` initialises the new `clock` field to a real-time source; only `with_clock` injects. Carried into Task 13's dispatch. Cost if wrong: compile error, caught immediately.

Ruling (F8): Task 14's harness test is implementer-authored beyond the sketch — it must register the project on both machines and produce a genuine divergence before asserting on `refs/remotes/lgs-auth/main`. An assertion that passes without a divergence would be the exact false negative the refspec bug hid behind. Carried into Task 14's dispatch. Cost if wrong: a test that passes vacuously; the final review should re-check it.

## Progress

Task 1: dispatched (base d540758), implementer returned DONE at 0ac0883.
Task 1: spike verdict — git2 0.19.0 with default-features=false clone/push/fetch refs/lgs-auth/* ALL OK against a live daemon. No exit needed; Phase 2/3 unchanged. Task 13 keeps its git2 implementation.
Task 1: implementer used a disposable scratch project rather than pushing a "spike" commit into a real project's synced history — good call, verified all five real projects still backed_up.
Ruling: `lgs-spike-scratch` remains in the Proton cloud root and shows under `adoptable`. `lgs forget` unregisters locally and leaves the cloud copy; lgs has no delete-from-cloud command, and hand-deleting directories out of the user's cloud storage is a destructive side effect outside this worktree that I will not take unasked. Surfacing it to the user at finish instead. Cost if wrong: one junk project listed in future `lgs projects` output; it does not match the `allowance-*` filter Task 19 uses, so it cannot reach the onboarding checklist.
Task 1: review dispatched over d540758..0ac0883, focused on whether the spike proves push rather than recording a false green.
Task 1: review verdict — spec ✅; quality: 1 Critical + 1 Important + 1 Minor.
  Critical: refs/lgs-auth/* fetch may have matched zero refs (reconcile runs on the daemon poll interval; test completed in 0.18s) — a zero-match fetch succeeds trivially, so "fetch OK" is indistinguishable from "nothing transferred". This is the false green the task warned about, on the exact mechanism Phase 2/3 depends on.
  Important: git2 Remote::push without a push_update_reference callback returns Ok even on per-ref server rejection; nothing confirmed the commit landed.
  Both verified against git/libgit2 semantics before accepting — not taken on the reviewer's word.
Task 1: fix round 1/5 dispatched (resumed original implementer) — prove push via callback + git ls-remote OID check; prove fetch by triggering a reconcile and polling with a bounded timeout, then asserting a real OID; correct the spec verdict to claim only what is demonstrated.
Task 1: fix round 1/5 result (commits 0ac0883..e820ef5) — Critical ADDRESSED (bounded 60s poll on git ls-remote, panics with an explicit message on timeout rather than skipping; fetched OID compared against an independently observed one, not a tautology). Important ADDRESSED (push_update_reference callback panics on rejection; independent ls-remote OID check against the real commit). Minor NOT addressed. New Important raised: spec text overclaims relative to the implementer's own report caveat.
Task 1: run-1 exit-101 adjudicated as non-blocking — display truncation lost the tail, two subsequent fully-logged runs passed consistently with no code change between them.
Task 1: fix round 2/5 dispatched — spec must record that reconcile is async, so the observed lgs-auth ref is not necessarily this run's own push. Matters because Task 15 argues from the spec that refs/lgs-auth/* is the merge input.
Task 1: Minor (finding 3) resolved by my own check rather than a round trip — `lgs-spike-scratch` does still appear under `adoptable`; folded into the round-2 message as a report disposition.
Task 1: fix round 2/5 (1 addressed, 0 open; commit e820ef5..637f63c). Spec now states the reconcile-async boundary with the concrete evidence (one run's poll found the previous run's commit) and scopes the claim to what Task 15 needs.
Ruling: verified the round-2 doc-only fix by reading the spec section myself rather than dispatching a re-review agent. The change was prose whose exact required content I had specified, and a full agent dispatch was disproportionate. Cost if wrong: an inaccurate paragraph in the spec; the final whole-branch review re-reads it.
Task 1: complete (commits d540758..637f63c, review clean after 2 fix rounds)

Ruling (pre-Task-2, my own defect): the `Money` Serialize impl I wrote into the plan routed through `serde_json::Number`, which is JSON-specific. `AllowanceConfig.amount` is an f64 that serializes to allowance_config.yaml, and although Task 3 does not convert it, a JSON-only Money impl would fail the first time money reached a YAML serializer. Changed to `serialize_f64`, which is format-agnostic and keeps the same wire shape. Cost if wrong: none identified; JSON output is unchanged (verified against the Task 2 and Task 3 assertions).
Task 2: dispatched on haiku (plan text carried complete code — transcription plus tests), base a81f0a2, returned DONE at 1672ad9. 6 money tests + no_io guard passing.
Task 2: review dispatched over a81f0a2..1672ad9, focused on money arithmetic and parsing edge cases the brief's own tests do not cover (empty string, bare "-", ".5", "5.", "+5", whitespace, non-numeric, i64 overflow) since every balance in the app flows through this type.
Task 2: review verdict — spec ✅; quality: 2 Important + 2 Minor.
  Important 1: FromStr `whole * 100 + frac_cents` is unchecked. Confirmed empirically by the reviewer: "922337203685477581" wraps to Money(20) in release, panics in debug. Silently wrong money is precisely what this crate exists to prevent. Defect originated in MY brief, not the implementer's transcription.
  Important 2: the no_io.rs guard I wrote scans five literal needles and would miss include_str!, std::env, std::io, and any third-party I/O crate — false confidence rather than enforcement.
  Reviewer hand-verified render() and FromStr across 500/-250/0/5/-5/-100/99/-99/i64::MAX and ""/"-"/"."/".5"/"5."/"+5"/" 5 "/"abc"/"5.x" — all correct.
Task 2: minor (deferred): render(i64::MIN) round-trips to an error rather than a value — same root cause as Important 1, and acceptable once it errors instead of wrapping.
Task 2: minor (deferred): proptest dev-dependency added but unused until Task 8. Intended.
Task 2: fix round 1/5 dispatched — checked_mul/checked_add returning Err; guard extended with more needles PLUS a dependency allow-list test, since the dependency list is the real I/O boundary and pinning it is stronger than any source scan.
Task 2: fix round 1/5 (2 addressed, 0 open; commits 1672ad9..2b81c44). Verified in debug AND release — the wrap only manifests in release, so a debug-only pass would not have proven it. Reviewer confirmed the multiply-overflow test genuinely reaches checked_mul, and that the remaining unchecked `frac.parse::<i64>() * 10` is provably safe (frac.len()==1 bounds it to 0-9, max product 90). Dependency allow-list test correctly scopes to [dependencies] and would fail by name on an added crate; [dev-dependencies] proptest does not trip it.
Task 2: minor (deferred): `fromstr_overflow_on_add_returns_err` is mislabeled — i64::MAX.99 overflows on checked_mul, so checked_add is never reached; it duplicates the multiply test. The genuine add-overflow path IS covered, by `i64_min_render_parses_back_to_err`. Risk if left: a future refactor deletes real coverage believing it is duplicated.
Task 2: minor (deferred): `i64_max_cents_renders_stably` asserts `m.render() == m.render()` — a tautology that only proves render() does not panic. Same false-confidence shape as the no_io guard defect, in the money type's own suite.
Ruling: both nits deferred rather than folded into another fix round. The reviewer graded them Minor and the loop's rule is that Minor findings never extend it; polishing here would be the "one more round" rationalisation the skill names. Both are recorded above for the final whole-branch review to triage, which is the mechanism that keeps this from being a silent discard. Cost if wrong: two weak tests reach merge; neither masks a functional gap, since the underlying requirement is genuinely covered elsewhere in the suite.
Task 2: complete (commits a81f0a2..2b81c44, review clean after 1 fix round, 2 minors deferred)
Task 3: dispatched on sonnet (multi-file integration), base 2b81c44. First attempt cut off by a session rate limit mid-migration; worktree held uncommitted work (transaction.rs, egui-frontend/Cargo.toml, new wire_compat.rs) and no commits. Resumed the same agent from its intact context; told it to commit whatever compiles if it hits another limit, so progress becomes durable.
Task 3: resumed agent returned DONE at 05d25ca. wire_compat 3/3; cargo test --workspace 0 failed across 20 test binaries. ~110KB diff.
Task 3: implementer flagged a scope-exceeding design decision — introduced Transaction::BALANCE_PENDING = Money::from_cents(i64::MIN) to replace an f64::NAN "not yet computed" sentinel, mapped back to NaN at the shared::Transaction DTO boundary.
Task 3: review dispatched with BALANCE_PENDING as priority 1. My specific worry, handed to the reviewer to settle: Task 2 proved render(i64::MIN) yields "-92233720368547758.08" which does NOT reparse (overflows i64, returns Err), and Task 5 turns an unparseable CSV row into a HARD ERROR with no fallback. So a persisted BALANCE_PENDING would create a file the app later refuses to read — a latent landmine armed by a task that has not run yet. Also asked whether it can reach the AWS wire, and what serde_json does with the NaN at the DTO boundary (NaN is not valid JSON).
Task 3: review verdict — spec ✅ (wire_compat 3/3 verified by the reviewer running it; shared::Transaction untouched as instructed); quality: 2 Important.
  Important 1 (LIVE REGRESSION): goal_service.rs:1096-1102 keeps BALANCE_PENDING on the projection-error path; goal_progress_graph/data_preparation.rs converts unconditionally, so the goal chart plots a ~-92 quadrillion point. Previously f64::NAN, which plotting treats as a gap. Introduced by this task.
  Important 2: containment is accidental. read_entity_for_sync serialises the RAW domain Transaction — it never routes through mappers::transaction_to_dto, so the NaN translation the report credited does not apply. Money::serialize_f64 would put a finite garbage float (~-9.223e16) on the AWS wire with no error.
  Reviewer settled my Task-5 landmine question: no reachable persist path TODAY, but nothing structural prevents one.
  (c) answered: serde_json 1.0.151 serialises NaN to JSON null rather than erroring; round-tripping that back into shared::Transaction.balance would fail. Pre-existing, not a regression.
Task 3: fix round 1/5 dispatched — skip BALANCE_PENDING in the chart data path; explicit guards at read_entity_for_sync and the store/write chokepoint so the value can neither be serialised nor persisted.
Ruling: rejected the Option<Money> structural fix the reviewer preferred, in favour of guards at the two chokepoints. Option<Money> would make the invariant compile-enforced, which is genuinely better, but it would touch the same breadth as Task 3 itself (which already ran ~2M tokens and 121 tool calls) and the guards close the actual reachable holes. Recorded as a known design trade-off for the final review. Cost if wrong: the invariant stays runtime-enforced, so a future call site can still construct the bad state and will fail at a guard rather than at compile time.
Task 3: fix round 1/5 (2 addressed, 0 open; commits 05d25ca..2def748). Chart skip verified at data_preparation.rs ~377-388 with a true absence test (asserts only the goal-start point survives, not a clamped value). Guards verified at BOTH chokepoints by reading the call sites, not the report: transaction_is_syncable() called inside read_entity_for_sync before serialisation; reject_pending_balance() as the first statement of both store_transaction and update_transaction, and upsert_transaction_from_sync delegates to store_transaction so it inherits it. Both guards return Err rather than substituting a value.
Task 3: minor (deferred): the app_coordinator guard test drives the extracted predicate transaction_is_syncable rather than read_entity_for_sync itself. Narrow coverage gap; the reviewer verified the call site by direct read.
Task 3: minor (deferred): Money's Add/Sub/Neg/Sum operators are unchecked (`Money(self.0 + o.0)`). Task 2's fix covered only FromStr. Unreachable at allowance scale — it needs ~9.2e16 cents — but it is an inconsistency with the crate's own no-silently-wrong-money thesis.
Ruling: the residual unguarded path the re-review flagged (update_transaction_balances -> write_transactions_internal, seeded by calculate_starting_balance) is NOT fixed in Task 3 and its loop is not extended. Task 4 deletes that path by construction: it rewrites recalculate_balances_from_date as a thin wrapper over the pure recompute_running_balances, which always accumulates from Money::from_cents(0) with no seed. Fixing it in Task 3 would be work Task 4 immediately discards. Carried into Task 4's dispatch as a requirement to verify rather than left to chance. Cost if wrong: if Task 4 somehow preserves a seeded path, the hole stays open — so Task 4's review must confirm the seed is gone.
Task 3: complete (commits 2b81c44..2def748, review clean after 1 fix round, 2 minors deferred)
Task 4: dispatched on sonnet, base 2def748, returned DONE at 1c008e0 then 3654ca3. allowance-core 15/15; workspace 20/20 binaries, 0 failures.
Task 4: implementer confirms no seeded accumulation remains — calculate_starting_balance deleted, recalculate_balances_from_date now always calls the pure recompute_running_balances from Money::from_cents(0) summing only amounts, so a BALANCE_PENDING balance can never reach an unchecked Add. This closes the residual gap carried out of Task 3; the review is verifying it independently rather than taking the claim.
Task 4: implementer flagged (correctly) that validate_all_balances swallows I/O errors as an empty mismatch list, implemented verbatim per my brief. Third defect of mine in the same false-confidence family: Err(Vec::new()) from a read failure reads to a caller as "validated, nothing wrong". Handed to the review as a directed item with instructions to grade it on merits rather than excuse it as plan-mandated.
Task 4: review dispatched over 2def748..3654ca3 with three directed items — the validate_all_balances error shape, (date,id) ordering in BOTH recompute and validate, and independent verification that no seeded/guard-bypassing write path survives.
Task 4: review verdict — spec ✅; directed items 2 and 3 both ✅ (the (date,id) tiebreak test confirmed non-vacuous — under a date-only stable sort the two input orderings would diverge and the assert would fail; and no seeded or guard-bypassing accumulation path survives, grepped independently). 1 Critical.
  Critical: validate_all_balances at balance_service.rs:244 conflates "could not read" with "read and found nothing" — Err(Vec::new()) from an I/O failure. No live caller yet, but the design doc frames this as the durable API.
Ruling (spec deviation, deliberate): replaced the spec's `Result<(), Vec<BalanceMismatch>>` with `anyhow::Result<BalanceCheck>` where BalanceCheck is a #[must_use] enum of Ok | Mismatches(Vec<..>). The spec's two-channel signature cannot represent three distinct outcomes, so patching the map_err would not have fixed it. The spec's binding intent — never return Ok when balances are wrong — is preserved, and the I/O conflation it did not consider is closed. The underlying error cause is now propagated rather than discarded. Cost if wrong: a signature that differs from the spec text; the spec should be amended to match before merge, and the final review should confirm the three outcomes stay distinguishable.
Task 4: fix round 1/5 dispatched.
Task 4: fix round 1 dispatch was killed by a session rate limit before writing anything; worktree clean at 3654ca3, nothing lost. Resumed the same agent with the full fix restated, plus an instruction to commit whatever compiles if it hits another limit.
Task 4: fix round 1/5 (1 addressed, 0 open; commits 3654ca3..17b541b). Verified the map_err is gone entirely rather than reshaped, and that the cause survives — the whole read chain (load_rows -> list_transactions_chronological -> read_transactions -> child_dir_for_create) is a bare `?` chain, so the original anyhow error reaches the caller unmangled. BalanceCheck is #[must_use] with structurally distinct variants.
Task 4: minor (deferred): the read-failure test asserts the error message is non-empty rather than matching the actual registry error, so it would still pass if the message became generic. Softer than it could be; mitigated because the path is provably a straight `?` chain.
Task 4: minor (deferred): BalanceCheck lives in balance_service.rs rather than beside BalanceMismatch in allowance-core. Defensible (the I/O-vs-pure split is the domain's boundary) but a judgment call.
Task 4: complete (commits 2def748..17b541b, review clean after 1 fix round, 2 minors deferred)

Ruling (privacy, pre-Task-5): the plan told Task 5 to copy the child's REAL transactions.csv into egui-frontend/tests/fixtures/ as a byte-stability corpus. This repo has a GitHub remote (github.com/kerryqhart/allowance-tracker), so that would commit a child's financial history — dates, descriptions, amounts — into git history permanently and push it to GitHub on the next push. That is a privacy decision for the user to make consciously, not one to take inside a plan step, and the round-trip property holds over any input. Amended the plan: the committed fixture is SYNTHESIZED to reproduce the real file's shapes (f64::to_string variants 5 / 5.5 / 27.25 and negatives, zero amount and balance, RFC3339 offsets across a DST boundary, all four legacy type values, a comma inside a description, rows out of canonical order), and the real file is exercised only by an #[ignore]d test reading a REAL_CSV env var so it never enters the repo. Cost if wrong: the committed corpus misses a real-world quirk nobody thought to synthesize — mitigated by the ignored test against the live file, whose result Task 5 must report.
Task 5: dispatched on sonnet, base fc2bcf2, returned DONE at f2df36a. allowance-core 21/21; workspace all green.
Task 5: REAL_CSV ROUND-TRIP FAILED — first error `unparseable money "14.620000000000001"`; 58 of 93 rows carry f64-precision leftovers from before Money existed. The implementer correctly refused to weaken Money::FromStr to make it pass. This vindicates the privacy ruling's ignored-test compromise: the synthesized fixture alone would have shipped a codec that refuses to open the user's real data on first run.
Ruling (load-bearing): resolve by adding Money::parse_rounding (round half away from zero, checked arithmetic, returns whether rounding occurred) used ONLY by the codec's read path; strict FromStr and its tests stay untouched. Rejected the two alternatives: relaxing FromStr loses the guard against genuine sub-cent claims everywhere else, and a separate normalisation migration adds an ordering hazard (a file that missed migration errors out) for no lasting benefit — render_transactions already emits two decimals, so the file is self-cleaning after one write. parse_rounding must reuse FromStr's digit/sign scanning rather than being a second parser, per Greg's codec-fork warning. Rounding count surfaces via StartupNotice so the user is told their data was normalised rather than it happening silently. Cost if wrong: the read path accepts a genuinely sub-cent value by rounding it; mitigated because money in this system is cents by definition and the normalisation is reported.
Task 5: fix round 1/5 dispatched (also folding in the 6-column ensure_transactions_file_exists header so HEADER has one definition).
Task 5: fix round 1 delivered at 190feb3. REAL_CSV now PASSES — 93 rows, 57 rounded on pass 1, 0 on pass 2, confirming the self-cleaning property empirically rather than by prediction. allowance-core 29/29; workspace green.
Task 5: interface change — parse_transactions now returns ParsedTransactions { rows, rows_rounded } rather than Vec<TxRow>, so the rounding count cannot be silently dropped. Implementer flagged the knock-on. Plan amended at three sites (Task 8's round-trip and timezone properties, and the file-structure table) to use `.rows`, committed, so Tasks 7/8/15 are not written against a stale signature.
Task 5: minor (deferred): the legacy-precision StartupNotice re-fires every startup until that child's file is next written. Correct in the sense that the on-disk data really is still unnormalised, but a ledger that is never edited again warns indefinitely. Option not taken: eagerly write the normalised file on load, which would make it fire exactly once — rejected for now because a startup write creates a commit before the user has done anything, and on two machines that is two normalisation commits racing. Left for the final review / user to weigh.
Task 5: review dispatched over fc2bcf2..190feb3 — priorities are whether parse_rounding truly reuses FromStr's scanning rather than forking a second money parser, whether it rounds negatives correctly (-5.005 must be -501) and keeps overflow protection, and whether all three silent-rewrite fallbacks are deleted rather than merely guarded.
Task 5: review verdict — spec ✅ (deviation disclosed), priorities 1/2/3 all satisfied. parse_rounding confirmed to share FromStr's scan() rather than forking a parser; -5.005 -> -501 verified by test and manual trace; overflow checked including the 99->100 carry; strict-vs-lenient divergence has its own test. All three silent-rewrite fallbacks confirmed DELETED (present only in comments). One writer, one HEADER definition. Fixture synthesized, real path appears only in the untracked report, nothing real in git.
  Important: rows_rounded is discarded by read_transactions on every everyday read; only startup's validate_all_transaction_files surfaces it. The BEHAVIOUR is correct and stays. The defect is that the comments claim the count "must never be silently absorbed" — an invariant nothing enforces, upheld by exactly one call site. Same false-confidence family as the unchecked overflow, the needle-scan I/O guard, and the empty-mismatch error earlier in this run.
Task 5: fix round 2/5 dispatched — correct the comment to state the real guarantee, and destructure `rows_rounded: _` at everyday-read call sites so the discard is visible rather than implied. No behaviour change.
Task 5: fix round 2/5 (1 addressed, 0 open; commits 190feb3..81f4502). Verified myself by reading the diff — docs plus an explicit destructure, identical test counts, no behaviour change. The comment now states plainly that rows_rounded is NOT structurally forced, names validate_all_transaction_files as the single call site holding the convention, and explains why ordinary reads discard it.
Ruling: verified this docs-only round by reading the diff rather than dispatching a re-review agent, same as Task 1 round 2. Cost if wrong: an inaccurate comment survives to the final review, which re-reads it.
Task 5: complete (commits fc2bcf2..81f4502, review clean after 2 fix rounds, 1 minor deferred)
Task 6: dispatched on haiku, base 81f4502, returned DONE at fb234d1. Transaction entropy fixed (rand::random::<u16>()), format preserved, dead shared::Transaction::generate_id deleted with grep evidence. 297 egui tests / 327 workspace pass.
Task 6: I caught a wrong conclusion in the report before review. The agent inspected backend/domain/models/goal.rs:43 and concluded Goal ids had no defect. That function is DEAD — `Goal::generate_id` has exactly one call site (goal_service.rs:134) resolving to shared::Goal::generate_id at shared/src/lib.rs:491, `goal::{child}::{millis}`, which has NO entropy at all. Same two-generators trap that made Greg's spec review cite dead code for transactions; the obvious-looking generator is the dead one in both cases.
Task 6: fix round 1/5 dispatched — add entropy to the LIVE shared::Goal::generate_id (rand goes in shared/Cargo.toml, NOT allowance-core), verify nothing parses goal ids first, delete the dead backend generator so two formats with different separators stop coexisting, and correct the wrong conclusion in the report.
Task 6: fix round 1 delivered at ac6f3b7 — live shared::Goal::generate_id now carries a rand suffix, dead DomainGoal::generate_id deleted (zero callers), no goal id parser exists so the format change is safe, 329 workspace tests pass.
Task 6: review dispatched over 81f4502..ac6f3b7. Priority 1 is an exhaustive inventory of EVERY generate_id in the workspace with live/dead and entropy status — the two-generators trap has now fooled two separate readers (Greg's spec review on transactions, this task's agent on goals), so I want certainty there is no third one lurking, including Child::generate_id which nobody has examined.

SPEC-COVERAGE GAP FOUND (not a Task 6 defect, recorded here so it is not lost):
The spec's merge section lists BOTH transactions.csv and goals.csv as row files merged by id. The plan implements merge over TxRow only — allowance-core models no goal row at all, and Task 7's merge signature is TxRow-specific. Two consequences: (a) goals.csv gets no semantic merge, so Task 15 must either merge it or explicitly refuse rather than silently picking a side; (b) DomainGoal.target_amount is still f64 (backend/domain/models/goal.rs:36), so goals.csv is still written via f64::to_string and has exactly the byte-instability that Money and the canonical codec removed for transactions.
Ruling: not fixing this inside Task 6, and not expanding Task 7 to a generic row trait — that would be speculative generality for a type that does not exist yet. Carrying it as a flagged gap into Task 7's dispatch (so its implementer does not over-build) and into Task 15's (which is where silently picking a side for goals would actually ship). The final review must triage whether goals merge lands in this branch or a follow-up. Cost if wrong: two machines editing goals diverge with no defined resolution, and goal money keeps the f64 rendering drift — both invisible until a second machine exists.
Task 6: review verdict — spec ✅. Transaction format still parses legacy ids; goal format change confirmed safe (grepped domain, storage, sync-service and UI — goal ids are opaque keys everywhere); rand correctly in egui-frontend and shared, absent from allowance-core.
  Important: BOTH entropy tests are flaky AND non-discriminating. >990 distinct out of 1000 draws from a 16-bit space sits at the birthday-paradox expectation (~7.6 collisions); the reviewer reproduced real failures (990, 989, 988) against correct code. Worse, the OLD clock-derived implementation would also have passed — nanos % 65536 in a tight loop yields ~990 distinct too, so the test never discriminated the defect it was written for. A test that goes green on the bug it targets is the false-confidence pattern again, now in the test suite itself.
  Priority 1 inventory delivered: 3 generate_id total. DomainTransaction (live, entropy), shared::Goal (live, entropy), shared::Child at shared/src/lib.rs:650 (DEAD, no entropy — real child ids come from a name-derived slug in child_service.rs, bypassing it). Third instance of the same trap, never flagged before.
Task 6: fix round 2/5 dispatched — replace both tests with an ordering-based discriminator (mint 200 ids at a FIXED timestamp, count descending adjacent suffix pairs; clock-derived gives ~0-2, uniform random ~100, assert >30), and delete the dead shared::Child::generate_id so the trap stops existing.
Task 6: fix round 2/5 (2 addressed, 0 open; commits ac6f3b7..34f9d00). The discriminator was verified the right way: the re-reviewer reproduced the OLD nanos%65536 algorithm in a throwaway and measured 0 descending steps on every run, vs a threshold of 30 and a random mean of ~99.5 (sigma~7) — so the new test provably fails on the bug it targets, which the previous test never did. Suffix parsing confirmed correct for both id shapes (parts[2] on `-`, parts[3] on `::`). Child deletion confirmed fully dead by grep across generate_id/parse_id/extract_timestamp/ChildIdError.
Task 6: fix round 3/5 dispatched — remove the `use std::fmt;` left unused by the ChildIdError deletion, plus a full warning sweep of the shared crate rather than just that line. Warnings introduced by our own diff get cleaned as we go rather than accumulating for the final review.
Task 6: fix round 3/5 (1 addressed, 0 open; commit 34f9d00..0214f03). Verified myself by reading the diff — a single `use std::fmt;` deletion, shared crate now builds warning-free.
Task 6: complete (commits 81f4502..0214f03, review clean after 3 fix rounds)
  Net effect: all three id generators inventoried; the two live ones (transactions, goals) now carry real entropy; all three DEAD duplicates deleted (shared::Transaction, DomainGoal, shared::Child) so the two-generators trap that misled three separate readers no longer exists.
Task 7: dispatched on sonnet, base 0214f03, returned DONE at 25be8da. All 10 brief tests pass; 37 unit + 2 guard tests, 0 failures. Implementer used the brief's code with no deviation and confirmed both fragile points (content-derived re-key, symmetric wins()).
Task 7: review dispatched on OPUS — deliberately the most capable model, because this is the one task everything in Phase 3 consumes without re-deriving, so a plausible-but-wrong merge is the most expensive defect available.
  Review framing: explicitly told NOT to check the code against the brief, because this session has repeatedly found defects that were IN my specification. Asked it to reason from first principles about symmetry across EVERY match arm (not just tested ones), the fixed point through the add/add case, whether dedup_by's consecutive-only behaviour is safe given the preceding sort, whether the unreachable!() arm is genuinely unreachable, and what happens when two different rows collide on BOTH the id and the 32-bit content suffix.
Task 7: review verdict (opus) — spec ✅ (code is verbatim the brief), quality ❌: 2 Critical + 2 Important. All four are defects in MY brief, faithfully transcribed. This is why the review was told to reason from first principles rather than check against the brief.
  Critical 1: reviewer reproduced real money loss with no clock skew — "book fair" destroyed, "slime" double-counted, audit log showing only TookTheirs. I traced it independently and the root cause is narrower than the review's framing: the edit/edit arm never checks whether one side is UNCHANGED relative to base, which is the core three-way rule. It goes straight to wins(), so an untouched peer with a later commit epoch beats a real edit. The re-key mechanism the review blamed is actually sound; adding the unchanged-vs-base check fixes the reproduction (verified by hand: base=A / R / A' totals the correct -400).
  Critical 2: dedup_by drops rows with no Decision recorded — the reviewer forced a genuine -999 transaction to vanish silently. Same false-confidence family, now in the merge's own audit trail.
  Important 3: intrinsic_eq compares dates by INSTANT, so same-instant/different-offset rows compare equal but render to different bytes. transaction_service.rs:101 writes Local::now().fixed_offset(), so offsets really do vary between machines. Two machines each keep their own row, render different bytes, and ping-pong forever — the exact non-convergence the design exists to prevent, arriving through a door neither the spec review nor the pre-flight scan checked.
  Important 4: wins() is asymmetric at fully-equal provenance (returns false both ways). Unreachable because equal oid implies the same commit, but nothing enforces it.
  Confirmed sound: (None,None) genuinely unreachable, oid comparison machine-independent, recompute at the right point, dedup_by's consecutive-only behaviour safe given the preceding sort.
Task 7: fix round 1/5 dispatched — add the unchanged-vs-base check, widen content_suffix to 64 bits and make dedupe emit DuplicateDropped, compare rendered dates in intrinsic_eq, debug_assert the provenance-inequality assumption, and add tests that assert on `decisions` (none of the existing 10 do) plus a one-sided-edit test that fails against the old code.
Task 7: fix round 1 delivered at 5918e22 — 48 unit + 2 guard tests, 0 failures (18 merge tests incl. 8 new, plus 2 new row.rs tests). All four findings reported fixed.
Task 7: re-review dispatched on OPUS over 25be8da..5918e22. Beyond verdicting the four findings I asked it to: independently re-verify the Critical 1 reproduction (rows surviving and the -400 total) rather than trust the new test; confirm the one-sided-edit test really gives the UNCHANGED side the HIGHER epoch, since a test that omits that would pass against the old code and prove nothing — the precise mistake Task 6 made; confirm the base-unchanged check uses intrinsic_eq rather than `==`, because `==` would compare balance and misjudge "unchanged" for any recomputed row; and re-walk SYMMETRY and the FIXED POINT, since the two new branches are asymmetric by construction (one takes t, one takes o) and must be exact mirror images.
Task 7: fix round 1/5 (4 addressed, 0 open; commits 25be8da..5918e22). Verification was unusually strong: the re-reviewer neutered the two new base-unchanged flags and confirmed exactly the three Critical-1 tests fail (empirical falsification, not inspection); brute-forced 2,574 symmetry scenarios (base None/empty/present x 6 row variants per side x extras x epoch and oid orderings, including same-instant-different-offset) with ZERO asymmetries; and 1,287 fixed-point scenarios plus a third application with ZERO violations. Confirmed the check uses intrinsic_eq not `==`, so balance is excluded; confirmed the dedupe rewrite keeps the same survivor as before (differential probe over 7 collision layouts); confirmed the one-sided-edit tests give the UNCHANGED side the higher epoch and so fail against the old arm.
Task 7: fix round 2/5 dispatched — an Important the re-review found in its OWN diff: cargo test --release now fails, because the #[should_panic] test targets a debug_assert! that is compiled out in release (47 passed / 1 failed). A suite that only passes in debug fails CI the first time anyone builds --release. Also adding `expected =` to that should_panic, and extending Decision::DuplicateDropped to carry the amount and date — a "loud" drop that records only a description is still a data-loss report you cannot act on, which defeats the reason we made it loud.
Task 7: note carried to Task 15 — a caller synthesising ONE placeholder Provenance for both sides would turn an add/add into a debug-build panic via the new debug_assert. No callers yet; Task 15 supplies real per-side provenance.
Task 7: fix round 2/5 (1 Important + 2 minors addressed; commits 5918e22..0305d5e). Verified myself by reading the diff: release test gated with #[cfg(debug_assertions)] and a comment explaining why, should_panic now carries expected = "identical provenance", and DuplicateDropped carries discarded_amount + discarded_date with the test asserting both. cargo test -p allowance-core 48 pass; --release 47 pass with the gated test correctly compiled out.
Task 7: complete (commits 0214f03..0305d5e, review clean after 2 fix rounds)
  Task 7 was the most valuable task of the run. The merge as I specified it destroyed real money with no clock skew involved, and it did so while passing all 10 of its own tests. Two independent convergence bugs (the missing three-way base-unchanged check, and intrinsic_eq comparing date instants while the app writes varying local offsets) would each have caused permanent divergence or silent loss in production, and neither was visible from the spec review or the pre-flight scan.
Task 8: dispatched on sonnet, base 0305d5e, returned PASS at 698872b. All 5 properties at 256 cases each plus the timezone test — 6/6 pass in BOTH debug (48 unit + 2 guard + 6 property) and release (47 unit, one cfg-gated test correctly compiled out). Zero shrinks, no property weakened, brief's deliberate choices (sort-before-dedup, integer cents) kept.
Task 8: review dispatched on OPUS. Central framing: six properties passing first try with zero shrinks is either genuine or a sign the generators cannot reach the interesting states, and a property that passes for the wrong reason is worse than none because it RETIRES the question. Asked for a per-property determination of whether row_strategy/sided_strategy can actually produce the edit/edit-with-base path, the add/add-no-base path (the one fixed_point exists for), the one-sided delete path, and equal epochs to exercise the oid tiebreak — quantified where possible. Also asked for EMPIRICAL falsification: inject "prefer ours" into wins() and a provenance-derived content_suffix, confirm symmetry and fixed_point actually fail, then revert and confirm clean. And whether the TZ test is vacuous given chrono may not observe a late set_var.
Task 8: review verdict (opus) — spec ✅ (verbatim the brief, integer cents, sort-before-dedup present, byte-level comparisons correct), quality: 1 Critical + 3 Important + 2 Minor. All defects in MY brief. The "six properties passed first try with zero shrinks" suspicion was justified.
  CRITICAL: fixed_point is STRUCTURALLY BLIND to the bug it exists for. It pins merged_side's provenance to a.provenance, so both merges see an identical (ours, theirs) provenance pair and ANY pure function of provenance yields the same suffix. Reviewer injected the provenance-derived content_suffix — the exact defect — and fixed_point PASSED at 256 AND at 200,000 cases. Only `symmetric` caught it. Note this retroactively undermines my own pre-flight ruling F3: I amended the re-key to be content-derived partly on the reasoning that Task 8's fixed_point would guard it. It would not have.
  IMPORTANT: generators cannot reach three paths. Per 200k cases — add/add re-key 2,585 (1.29%); edit/edit both-changed 25 (0.0125%); one-sided-edit-beats-untouched 0; Deleted decision 0; equal-epoch-with-real-conflict (oid tiebreak) 0; DuplicateDropped 0. The zero rows include the one-sided-edit path, which is exactly where Task 7's money-loss bug lived. Cause: sides generated independently, so base.intrinsic_eq(side_row) essentially never holds.
  IMPORTANT: sided_strategy can emit identical provenance, tripping wins()'s debug_assert; proptest shrinks TOWARD zero, so a genuine future failure tends to shrink into "identical provenance" and misreport its own cause — the reviewer hit exactly this.
  IMPORTANT: the TZ test is vacuous — single offset in the input, parse/render never consult chrono::Local, and set_var without tzset would not move cached state. It applies the same pure function to the same string twice, and leaks TZ process-wide.
  Empirical falsification performed and reverted: wins->true made `symmetric` fail at case 20; provenance-derived suffix made `symmetric` fail but `fixed_point` PASS at 200k; removing recompute failed `idempotent` + `balances_validate`. Worktree confirmed clean afterwards.
Task 8: fix round 1/5 dispatched — fresh provenance for the merged side (with instructed self-falsification), base-derived side generation via prop_oneof![keep, edit, delete, add] so unchanged-vs-base becomes common, tiny epoch range for oid-tiebreak coverage, provenance-inequality by construction, a real same-instant-different-offset test replacing the TZ theatre, CSV special characters in the round-trip corpus, and 5,000 cases for fixed_point/symmetric. Required deliverable: instrumented hit rates for all six paths, numbers not assurances.
Task 8: fix round 1/5 delivered at dcfac6c — all six findings addressed. Path hit rates over 20,000 cases, transformed: add/add rekey 1.29% -> 86.5%; edit/edit both-changed 0.0125% -> 46.3%; one-sided-edit-wins 0 -> 91.3%; Deleted 0 -> 30.3%; equal-epoch oid tiebreak 0 -> 15.3%. DuplicateDropped remains 0 organically (hash-collision-only path) and is covered instead by a dedicated constructive property at 100%. CRITICAL 1 verified by self-falsification: injected a provenance-pair-derived suffix, confirmed BOTH fixed_point and symmetric fail with a shrunk counterexample, then reverted (merge.rs confirmed clean). Debug 48+2+9 green; release 47+2+9 green.
Task 8: fix round 2/5 dispatched — the implementer honestly flagged that DuplicateDropped coverage relies on hand-duplicated copies of merge's private content_suffix/wins in the integration test file. Ruling: move that one constructive test into the #[cfg(test)] mod inside src/merge.rs, where a unit test can reach private items, and delete the copies. Explicitly told NOT to widen content_suffix/wins to pub for a test's convenience. This project has already been bitten twice by duplicate-implementation traps (two live/dead id generator pairs, and nearly a second CSV parser per Greg's warning), so a second copy of the merge's own algorithms is not a residual worth accepting.
Task 8: fix round 2/5 delivered at 1f3389f — DuplicateDropped test relocated into merge.rs's #[cfg(test)] mod calling the real private content_suffix/wins; duplicated copies deleted; neither function made pub. Implementer self-falsified again unprompted (disabled the orphan injection, confirmed only KeptBothReKeyed fired and the test failed, reverted).
Task 8: re-review (opus) over 698872b..1f3389f — ALL findings ADDRESSED, verified independently rather than accepted:
  Own injection of a deliberately SYMMETRIC fixed-point defect made fixed_point FAIL while the other 6 passed. Then the ablation that settles it: reverting ONLY the test's provenance line while leaving the defect in merge.rs made fixed_point PASS 5,000 cases. The fresh provenance is provably the load-bearing change.
  Hit rates reproduced within noise (87.3 / 45.5 / 93.4 / 30.1 / 14.8 vs reported 86.5 / 46.3 / 91.3 / 30.3 / 15.3).
  Deletes confirmed genuine, not empty-side artifacts: 5,410 of 5,442 delete-bearing cases had both sides non-empty; only 71/20,000 cases had any empty side.
  Identical cross-side provenance now structurally impossible (disjoint oid domains 0..80 / 100..200). sided_strategy still overlaps but feeds only `idempotent`, where wins() is unreachable — correct.
  TZ theatre replaced with a real same-instant/different-offset test asserting both side orders converge to identical bytes.
Task 8: fix round 3/5 dispatched — the "edit beats delete" branch emits no Decision. Reviewer graded it Minor as an instrumentation gap; I am treating it as an AUDIT gap. That branch resurrects a row the other machine deliberately deleted, and the deleting user currently gets no record that their deletion was undone. Same principle that made DuplicateDropped loud. Adding Decision::EditBeatDelete, instrumenting it, and asserting it in the delete tests.
Ruling: explicitly NOT fixing the round-2 oid-tiebreak gap (merged_side always wins round 2 since fresh_epoch > theirs). The tiebreak is already exercised at ~15% in round 1, and contriving a round-2 tie would complicate the property for no added coverage. Cost if wrong: none identified — the code path is covered elsewhere.
Task 8: fix round 3/5 (1 addressed; commits 1f3389f..553387f). Verified myself by reading the diff — EditBeatDelete pushed from BOTH arms and correctly gated on `base_known && b.is_some()`, so a plain add is not misreported as a resurrected delete; both directions unit-tested with decision assertions; instrumentation now labelled "decision counts, not per-case rates". Hit rate 6,095/20k debug, 6,023 release, matching the reviewer's independent 6,074. Debug 50+2+8 green; release 49+2+8 green.
Task 8: complete (commits 0305d5e..553387f, review clean after 3 fix rounds)

=== PHASE 1 COMPLETE (Tasks 1-8) ===
The pure correctness core is finished and provable with no daemon, no network, no filesystem:
  - exact integer-cent Money with checked overflow, wire-compatible with the MCP Lambda
  - one canonical CSV codec that reads the user's real 93-row file (57 legacy-precision rows normalised on first read, 0 on second)
  - a three-way semantic merge that converges, with every meaningful outcome logged
  - five byte-level properties plus a same-instant/different-offset test, over generators proven to reach every path
Every one of the 8 tasks required at least one fix round. 16 defects total, all of which compiled and passed their own tests.
Task 9: dispatched on sonnet (chose sonnet over haiku deliberately — this is the safety-critical predicate, and haiku made the wrong-generator error in Task 6), base 553387f, returned DONE at 940a5e6. 6/6 new tests including all five edge cases I added beyond the brief; workspace green.
Task 9: review dispatched over 553387f..940a5e6, aimed at FALSE NEGATIVES specifically — a path wrongly ACCEPTED is the dangerous direction, since it means silent .git corruption later. Directed at the three most likely holes: `..` traversal that escapes into a cloud location after normalisation (Path::starts_with does not see through `..`); a symlink whose target is cloud-synced (the guard is pure and cannot resolve those — is the caller-side responsibility documented?); and case-insensitivity, since macOS filesystems are case-insensitive by default while Path::starts_with is case-SENSITIVE, so a lowercase `/users/.../library/mobile documents/` variant may sail through.
Task 9: review verdict — spec ✅ (purity confirmed by grep: no dirs::, fs:: or Command:: inside the predicate; injected home field present; Finder-pref reasoning documented; all ten table cases literal, none computed via the function under test). 1 Critical + 2 Important, ALL false negatives — the dangerous direction.
  Critical: `..` traversal accepted. Path::starts_with never interprets ParentDir, confirmed by a compiled probe — `<children_root>/../../../Library/Mobile Documents/x` returns None. No normalisation, no documented precondition, no test.
  Important: case sensitivity. macOS default APFS is case-insensitive but starts_with is case-sensitive, so `/users/k/library/mobile documents/x` — literally the same directory — is accepted. Confirmed by probe.
  Important: the symlink limitation is undocumented for the `candidate` argument; existing prose covers only the unrelated documents_is_symlink flag.
Ruling: fix by lexical normalisation inside the guard (pure — explicitly NOT canonicalize(), which would hit the filesystem and destroy the testability that makes this predicate trustworthy), plus FAIL CLOSED on anything unresolvable via a new Reason::NotNormalisable. A guard that cannot prove a path safe must say no.
Ruling: compare case-insensitively, accepting that on a genuinely case-sensitive volume this yields a false POSITIVE (an inconvenience) rather than the current false NEGATIVE (data corruption). Direction of the trade recorded in a comment so it is not silently reversed. Cost if wrong: a user on a case-sensitive volume with two paths differing only in case sees a spurious rejection they can work around by renaming.
Task 9: fix round 1/5 dispatched.
Task 9: fix round 1/5 (1 Critical + 2 Important addressed; commits 940a5e6..13958eb). Independently verified: normalize_lexically pops only when the stack top is Component::Normal, so a pop above RootDir genuinely returns None -> Reason::NotNormalisable rather than silently yielding an empty path that matches nothing (the false negative in new clothes). Case-insensitive comparison is component-by-component, not whole-string, so the prefix-sibling case survives — confirmed by test AND by the reviewer's own probe (/x/Code vs /x/CODEOTHER/child accepted; /X/CODE/inside rejected). Normalisation computed once up front and applied to all three checks, so no partial-fix branch. Purity re-confirmed: canonicalize/fs::/dirs::/Command:: appear only inside doc comments explaining what not to do. 16/16 sync::paths tests; workspace green, 313 in the egui crate.
Task 9: complete (commits 553387f..13958eb, review clean after 1 fix round)
Task 10: dispatched on sonnet, base 13958eb, returned DONE at 03687fb. Fixture captured from lgs commit a2eb77f. 6/6 new tests; workspace green.
Task 10: LIVE OPERATIONAL FINDING (surfaced to the user, awaiting their call): `lgs status --json` shows allowance-tracker in durability state "stranded" — label "Not saved anywhere else — lgs-desktop-sync has commits from 2026-09-07 that exist only on this machine", branches lgs-desktop-sync AND main, next step `git push lgs lgs-desktop-sync`. Every commit of this session — all implementation on the branch, plus the spec/memos/plan I committed to main — exists on one disk. The other four projects are backed_up. Not pushing on my own initiative: publishing to the user's Proton cloud root is a side effect outside this worktree.
Task 10: review verdict — spec ✅; 2 Important + 1 Minor.
  Important: 3 of lgs's 7 durability states collapse to Unknown, and ALL THREE are Severity::Red — stranded, working_repo_unreadable, bare_repo_unreadable. "Not backed up anywhere else" is currently indistinguishable from "a value added after we shipped".
  Important: can_claim_durability() gates on daemon freshness only and is report-wide, so it returns true for a report containing a genuinely stranded project. The name invites the next author to read it as a per-project safety answer.
  Minor: the fixture test asserts only that projects is non-empty, leaving the stranded discovery in prose rather than pinned by a test.
  Confirmed good: daemon enum fully covers lgs's four states; malformed JSON errors rather than panics; minimal project entry parses; no trait added; run() surfaces stderr; fixture provenance comment present and matching lgs HEAD.
Task 10: fix round 1/5 dispatched — model all seven states from lgs's source (not guessed), rename to durability_data_is_fresh() with a doc comment saying it is NOT per-project safety, add ProjectReport::is_confirmed_backed_up() that returns false for Unknown (an uninterpretable state must never render as safe, since three of them are Red), and pin the stranded fixture fact as a regression guard.
Task 10: fix round 1/5 (2 Important + 1 Minor addressed; commits 03687fb..473472d). Wire strings verified character-by-character against lgs's own durability_tags! macro (health.rs:1192-1200), which is generated from an exhaustive match — the strongest ground truth available. All seven map exactly, no state silently rerouted to Unknown. is_confirmed_backed_up() is `matches!(.., BackedUp)`, false for Unknown as required. Old name fully gone (grep: zero hits). Fixture pins Stranded specifically, not merely "not BackedUp". The table test iterates all eight variants with per-case expectations, not spot checks. No exhaustive match elsewhere consumes DurabilityState, so the added variants break nothing.
Task 10: also amended the PLAN myself — its embedded Task 10 snippet still showed can_claim_durability, and briefs are extracted from the plan, so a later task would have copied the stale name verbatim. Added the doc comment explaining the distinction at the same time.
Task 10: complete (commits 13958eb..473472d, review clean after 1 fix round)
Task 11: dispatched on sonnet, base 473472d, returned DONE at 26f6f13. 6/6 bootstrap tests; workspace green. Implementer honestly flagged it could not verify the cargo-bundle resources path without producing a real .app (cargo-bundle not installed, no pinned lgs release binary yet) — it verified the dev-build half of build.rs only.
Task 11: review dispatched over 473472d..26f6f13. Priorities: (1) is the copy genuinely atomic, is the temp on the SAME filesystem as the destination (fs::rename across filesystems fails on macOS, and a test using one tempdir for both would pass while production fails), are permissions set BEFORE the rename, is the temp cleaned up on failure; (2) is the .app path arithmetic right for Contents/MacOS/foo -> ../Resources/lgs, and what happens when neither location exists; (3) does git_is_available() distinguish "git printed a version" from "the Xcode CLT stub spawned and exited non-zero" — the stub spawns fine, so a spawn check is not enough. Also asked it to grep the diff for any reappearance of the false "no command-line dependencies" claim from my earlier spec draft.
Task 11: review verdict — spec ✅. Confirmed SAFE on the two traps I aimed at: the temp is dst.with_extension("tmp") so same-directory, avoiding the cross-filesystem rename failure that would pass tests and fail in production; and the .app path arithmetic is correct. 1 Critical (unverifiable) + 2 Important + 1 Minor.
  Critical: cargo-bundle resource placement genuinely unverifiable here (tool not installed, and running it was out of scope). Risk: `resources` mixes an in-crate path with `../target/release/lgs`, and if cargo-bundle strips relative to a common ancestor, lgs lands at Contents/Resources/target/release/lgs instead of the flat path the resolver expects — every release build broken, every test green.
Ruling: rather than verify the convention or defer the unknown to Task 12, make the code TOLERATE the variation. bundled_lgs_path() now searches an ordered candidate list (flat Resources, nested target/release under Resources, dev-build sibling) and, on total failure, errors with every path it tried so a packaging mistake is diagnosable from the message alone. Converting an unverifiable assumption into a tolerated variation is the right trade when being wrong means broken releases with a green suite. Cost if wrong: a few extra stat calls at startup.
  Important: copy_binary leaks its .tmp on rename failure. Important: build.rs swallows fs::copy errors via `let _`, so a bad LGS_BINARY fails silently at build time and surfaces as a confusing runtime error. Minor: git_is_available test only println!s, cannot fail.
Task 11: fix round 1/5 dispatched. Note on build.rs: warn+fail ONLY when LGS_BINARY is set and the copy fails; stay silent when unset, so an ordinary cargo build for someone who just wants to run the app keeps working.
Task 11: fix round 1/5 (1 Critical + 2 Important + 1 Minor addressed; commits 26f6f13..cd8d18b). Candidate search confirmed first-match in the specified order (not last-match, which could have picked a stale dev binary over the bundled one); the failure message names all three candidates and the test asserts each string rather than is_err(). Rename failure preserves the ORIGINAL io::Error — cleanup result discarded via `let _`, context applied to the original — verified by a test that forces failure with a pre-existing directory at dst. build.rs correctly distinguishes unset (silent return) from set-and-failed (cargo:warning naming source and dest, then panic); reviewer ran it live both ways and confirmed a plain `cargo build` with the var unset still succeeds and the cache is not wedged.
Task 11: complete (commits 473472d..cd8d18b, review clean after 1 fix round)
Task 11: carried forward — which candidate a REAL `cargo bundle` populates is still unconfirmed. The code now tolerates all three, so this is no longer a correctness risk, but the first real release build should check which one is used.
Task 12: dispatched on sonnet with a HARD safety constraint (no install-service/launchctl/mutating lgs commands — the user has a healthy daemon backing up five real projects). Base cd8d18b, returned DONE at ced10ba. 4 brief tests + 10-case decision table + 2 persistence tests; workspace 335 passed, 0 failures. Implementer confirmed it ran no service-mutating command.
Task 12: implementer found a real bug beyond its brief — sync_thread.rs's save loop reconstructs SyncState every ~30s and would have reset daemon_ownership to false on every save, silently un-owning a daemon the app installed. Threaded it through.
Task 12: review verdict — spec ✅, approved. Save-loop fix confirmed real AND complete (only two SyncState literals exist, both updated; only one production save site; app_state.rs loads but never saves). Label verified against lgs source: LAUNCHD_LABEL "com.local-git-sync.daemon" matches exactly, plist path matches service::unit_dir()/unit_filename(), so bootstrap targets the real unit rather than silently no-oping. kickstart failure is gated by anyhow::ensure!, so install_and_start cannot return Ok with no daemon. uid via direct spawn, no shell chain.
Task 12: deferred minor — the SyncState reconstruction pattern is "self-alerting but not self-protecting": adding a field forces E0063 at every literal, but that error can be resolved correctly OR with Default::default(), silently reintroducing this bug. Reviewer recommends a mutate-in-place model (hold one long-lived SyncState, update watermarks, save it) so new fields persist with no code change. Worth doing next time this file is touched; flagged for the final review.
Task 12: deferred minor — `Down + installed_by_app: true -> InstallAndOwn` conflates "never installed" with "our daemon crashed". It works (install-service is idempotent and re-points the plist at the stable binary path, which is self-healing) but the action name is imprecise. Brief-level ambiguity of mine, not an implementer defect.
Task 12: deferred minor — bootstrap.rs computes the plist path from dirs::home_dir() while lgs uses a $HOME-env-based home_dir(); these normally agree but are not guaranteed identical. Also no post-kickstart health poll. Both Minor: the kickstart exit-status gate already prevents the primary "Ok with no daemon" failure.
Task 12: complete (commits cd8d18b..ced10ba, review clean, 3 minors deferred)

PLAN COVERAGE GAP (mine, recorded before it bites): the brief for Task 12 named `ensure_daemon`/`DaemonOutcome` in its Interfaces line but gave no code, and the implementer correctly declined to invent it. The reviewer confirmed nothing in the diff needs it. The deeper gap: NO TASK IN THE PLAN WIRES APP STARTUP. The spec has a "First run" section (folder picker -> lgs init -> git check -> daemon adopt-or-install -> register children) but tasks 11/12 only produce the pieces; nothing calls ensure_lgs_binary, plan_daemon_action, install_and_start or is_cloud_synced from real startup code.
Ruling: fold first-run wiring, including `ensure_daemon` as its composition point, into Task 19 (onboarding), which is the task that already owns the first-run UI surface. Recording rather than silently leaving the pieces unwired. Cost if wrong: Task 19 grows; the alternative — a separate task — is cleaner but the two overlap heavily on the same UI and the same LgsClient calls.
Task 13: dispatched on sonnet, base ced10ba, returned DONE at 514e172. 10/10 git-module tests; workspace green. Spike resolution carried into the dispatch so the implementer used git2 rather than re-litigating the transport question.
Task 13: review verdict — spec ✅, approved, 1 Important + 1 Minor. Refspecs verified verbatim: "+refs/lgs-auth/heads/*:refs/remotes/lgs-auth/*" and "+refs/heads/*:refs/remotes/lgs/*". Leading `+` present on both (without it a non-fast-forward peer tip is refused and the merge input goes stale — the original stall through a different door); destinations disjoint so the heads mapping cannot shadow the auth one; names verified against lgs source at engine.rs:20,399. The fetch test writes a REAL refs/lgs-auth/heads/main via git2 and asserts refs/remotes/lgs-auth/main resolves to that exact OID — a genuine regression test, not the false-green Ok check that bit the Task 1 spike. Push rejection: push_update_reference callback wired, rejection maps to Err naming the ref and message, and the test induces a real non-fast-forward and asserts both Err AND that the bare ref did not move.
Ruling (answering my own plan gap): `merge_base` is NOT needed — Task 15's code calls repo.merge_base() directly via git2 and a wrapper adds nothing; my brief's Produces line was wrong to list it, and the implementer was right to omit it. `commit_merge` IS needed and belongs on GitManager, not Task 15: GitManager holds the injectable clock, and that clock exists so tests can construct committer-timestamp TIES — the only way the merge's tie-break-by-oid branch is coverable. Implementing it in Task 15 would duplicate the clock plumbing and quietly make those tests unconstructible. Adding it here while the implementer has context. Cost if wrong: GitManager gains a method Task 15 turns out not to use.
Task 13: fix round 1/5 dispatched.
Task 13: fix round 1/5 (1 Important addressed; commits 514e172..6a64303). commit_merge verified: self.signature() passed as BOTH author and committer, and the test asserts commit.committer().when().seconds() specifically — the field that matters, since the merge's resolution rule reads committer time. Asserting the author field instead would have passed while leaving the injected clock doing nothing for tie construction. Zero parents bails before any repo interaction, so git2 never gets the chance to create a root commit. Tree built from index.write_tree() after add_all, with a test writing merged.txt immediately before the call and asserting its exact bytes in the resulting tree — a real catch for "tree from HEAD instead of index". Parent oids resolve via Oid::from_str + find_commit with errors propagated, no unwrap. 13/13 git tests; workspace 345 passed, 0 failed.
Task 13: complete (commits ced10ba..6a64303, review clean after 1 fix round)
Task 13: note carried to Task 15 — commit_merge always moves HEAD (mirroring existing commit()), so Task 15 must call it only when the working repo's checked-out branch is the merge target, and both parent oids must already be reachable in the ODB.
Task 13: deferred minor — push rejection was verified against a local file-transport bare repo only, not smart-HTTP. Acceptable: the Task 1 spike already verified smart-HTTP push against a live daemon WITH a rejection callback, so both halves are covered, just not in one test.

=== PHASE 2 COMPLETE (Tasks 9-13) ===
Guard, LgsClient, binary bundling, daemon ownership, and git remote operations all landed. Phase 3 (the sync loop) is next.
Task 14: dispatched on sonnet with an explicit isolation contract (own HOME, own cloud root, own port 18418/18419 never 8418), base 6a64303, returned DONE at 4b4e433. The convergence test passes in 1.83s and proves the real thing: on machine B, refs/heads/main stays at B's own commit while refs/remotes/lgs-auth/main resolves to A's tip after a genuine divergence. That is the original bug reproduced in a harness and now guarded in CI without a second Mac.
Task 14: I independently verified the user's real daemon after the run — ok on 8418, cloud root intact, all five projects present with UNCHANGED generations (weathertop-brain 5, weathertop-data 7, kerry-ai-config-personal 5, local-git-sync 16). Nothing disturbed.
Task 14: review verdict — spec ✅; all three disclosed deviations verified sound against lgs source. Isolation verdict: no path found that touches the real daemon, cloud root, or launchctl. 3 Important.
  Important 1: THE TEST MAY NOT PROVE WHAT IT CLAIMS. run_sync_loop ticks every poll_interval_secs (default 30, never overridden) and the test's bound is 60s, so ambient daemon ticks could converge B even if sync_both_ways were a no-op. Exactly the "could this pass if sync did nothing" question I asked, answered badly.
  Important 2: the report's "every lgs call funnels through one function" claim is FALSE — the daemon spawn and readiness check each build their own Command. Both do set HOME (verified), so isolation holds, but the property is documentary rather than structural. Since HOME isolation is the only thing between this harness and the user's real projects, documentary is not good enough.
  Important 3: Drop-registration gap — the daemon is held as a bare std Child (no-op Drop) during the readiness check, before Machine is constructed, so a readiness failure leaks a daemon holding a test port and breaks the next run.
Task 14: fix round 1/5 dispatched — patch poll_interval_secs to 3600 so ambient ticks cannot fire and explicit sync is provably the driver (with a comment so nobody tidies it back), THEN verify by making sync_both_ways a no-op and confirming the test fails; make HOME isolation structural via a single lgs_command() used by every call site including the spawn; construct Machine immediately after spawn so Drop is armed before the readiness check; and run the test three times to surface flakiness or leaked orphans.
Task 14: fix round 1 delivered at cb04bd0. Three real runs: FAILED (genuine 60s convergence timeout), PASSED 1.81s, PASSED 1.87s. No-op verification WORKED — commenting out sync_both_ways made the test fail on the bound, proving the assertion cannot pass vacuously. Real daemon confirmed untouched before/after every run including the failing one; no stray processes or held ports. Implementer reported the 1-in-4 failure rather than masking it by raising the timeout, which is what made it diagnosable.
Task 14: I diagnosed the flake myself. `lgs sync` IS NOT SYNCHRONOUS — cli.rs:915-934 sends Request::SyncNow and prints "sync triggered.", returning on ACKNOWLEDGEMENT, not completion. So sync_both_ways's fixed sequence (A, B, A) races: B's sync can fire and finish before A's publish lands. The old 30s default poll meant an ambient tick cleaned that up inside the 60s bound, which is exactly why the test passed before and flakes now. My poll_interval_secs=3600 change did not cause the race; it removed the net that was hiding it. The flake is the truth surfacing.
Task 14: fix round 2/5 dispatched — replace the fixed three-call sequence with a BOUNDED RETRY LOOP that triggers explicit syncs on both machines until the condition holds or the deadline expires, keeping poll_interval at 3600 so ambient ticks still cannot be the driver. Required: five consecutive runs (3 is thin evidence for a 1-in-4 defect) and a REPEAT of the no-op verification, because a retry loop that accidentally passes without syncing would be worse than the flake — stable-looking and proving nothing.
LGS CONTRACT FINDING (worth carrying beyond this task): `lgs sync` returns on acknowledgement, not completion. Anything needing a sync to have FINISHED must observe the result rather than assume it. Relevant to Task 15 and to any later code that triggers a sync and then reads state.
Task 14: fix round 2 delivered at 5c815cd. FIVE consecutive passes (3.69/3.67/3.70/3.67/3.67s), no stray processes or held ports after any run, real daemon untouched throughout. No-op verification repeated and still correctly FAILS on the bound.
Task 14: the implementer improved on MY fix, which is worth recording. My suggested interleaved retry loop (nudge both machines each round) was itself racy: interleaving let either side win the publish race while the test only accepts B mirroring A. Diagnostic logging caught A stuck "diverged" for a full 60s in one failing run — the reverse of the required outcome. It replaced this with two CONFIRMED sequential phases: retry A alone until A's own status reports backed_up, only then retry B alone until it mirrors A. 5/5 clean versus 0-1/2 on my interleaved version.
Task 14: disclosed limitation, honestly stated — the two-phase design's race-freedom rests on CONSTRUCTION (phase 2 cannot start until phase 1 confirms) rather than on an adversarial test proving it under hostile timing. Asked the re-review to judge whether that is acceptable or whether a specific extra test would raise confidence meaningfully.
Task 14: re-review was killed mid-flight by a session rate limit after it had verified the isolation mechanisms in code; resumed it with the real daemon's current generations supplied so it can confirm nothing was disturbed, and told it that running the harness is optional given five reported passes plus a successful no-op check.
Task 14: re-review PASSED (code-only; reviewer judged isolation solid from source and declined to spend another daemon spin-up given 5 reported passes plus two no-op verifications). All three original Importants plus the ACK-only race fixed BY MECHANISM:
  poll_interval_secs=3600 written unconditionally by patch_config before any daemon spawn, named constant with a comment forbidding tidying it down; the no-op check genuinely fails at 60s, confirming ambient ticks are not masking anything.
  Exactly ONE Command::new in the test code, inside lgs_command(); run, daemon spawn and wait_ready all route through it. Isolation is structural, not conventional.
  Machine::start assigns m.daemon = Some(child) on the line immediately after spawn, with no fallible operation in between, so Drop is armed before readiness can fail.
  Phase 1's confirmation verified MEANINGFUL by reading lgs's health.rs: BackedUp is only reported when every branch is UpToDate against the cloud's authoritative manifest, and a push in flight reads as Pending. So phase 2 cannot begin while the publisher's commit is unlanded — the guard is real, not a status read that succeeds too early.
  All three substantive assertions (auth == a_tip, b_head == b_tip, auth != b_head) can genuinely fail.
Task 14: disclosed limitation judged ACCEPTABLE by the reviewer — race-freedom is a structural guard rather than a timing convention, and an adversarial test would be disproportionate for a harness. Cheapest useful future addition recorded: a unit test asserting retry_sync_until never issues sync on the ingester before is_backed_up(publisher) returns true.
Task 14: closing round dispatched for one Minor — wait_for_auth_tip's panic text still describes the abandoned interleaved v2 algorithm. That message is the FIRST thing seen when this test fails in CI, and describing an algorithm the code no longer uses would send a debugger hunting a race that is not there. Not cosmetic for a harness.
Task 14: closing round delivered at b6ce84a (string-only wording fix). Implementer ran two confirmation runs rather than the one asked, because the first was unexpected: run 1 FAILED on the 60s bound (phase 2 demonstrably ran and observed changing ref values, just never the expected one), run 2 PASSED in 3.61s. Cleanup clean after both, real daemon untouched. Net across all v3 testing: 7 of 8 real runs passed, versus ~1-in-4 failures on v1 and 0-2 of 2 on v2.
Ruling: ACCEPT the residual flake as a documented limitation rather than spending further rounds on it. Reasoning, in order of weight: (1) the test is DOUBLE-GATED — #[ignore] plus a required LGS_BINARY env var — so an ordinary cargo test never runs it and it cannot break CI or a normal build; (2) the product-level property it guards (a fetch landing the peer tip at refs/remotes/lgs-auth/* with the right OID) ALSO has a deterministic, non-flaky unit test from Task 13 against a local bare repo, so the guarantee is not resting on the flaky test; (3) the observed failure mode is "did not converge within the bound", never "converged to the wrong value", so it cannot produce a false PASS; (4) the structural race-freedom was independently verified against lgs's health.rs. I deliberately did NOT raise the timeout — that was forbidden earlier as masking, and the distinction still holds: I am not confident the residual is latency rather than a logic gap, so widening the bound would be hiding an unknown rather than accommodating a known. Cost if wrong: an opt-in harness test occasionally fails for someone running it deliberately; they will see a bounded, descriptive failure rather than a wrong answer.
Task 14: follow-up recorded for the final review — investigate the residual v3 flake (phase 2 runs and observes changing values but never reaches A's tip within 60s). Cheapest useful additions named by the reviewer: a unit test asserting retry_sync_until never syncs the ingester before is_backed_up(publisher), and instrumentation of what lgs's reconcile does on the failing round.
Task 14: complete (commits 6a64303..b6ce84a, review clean after 3 rounds, 1 residual flake documented and contained)
Task 15: dispatched on sonnet with all four hard-won facts carried in (refspec is the design; provenance must be per-side and real or wins()'s debug_assert panics; commit_merge moves HEAD and needs parents reachable in the ODB; lgs sync is ack-only). Base b6ce84a, returned DONE at 9782263. allowance-core 60/60, egui 353/353 plus 8 new child_sync tests, sync-service 30/30. Local bare repos only; no real lgs binary or daemon touched.
Task 15: goals.csv handled as required — goals_diverged() byte-compares it between the two diverged tips and apply_merge logs a loud warning naming the child and BOTH parent oids before committing, rather than silently carrying whichever copy is checked out. That was the one place this task could have destroyed data outside its own scope.
Task 15: review dispatched on OPUS. Priority 4 is a loop hazard the implementer disclosed honestly: Cycle::Diverged ALSO fires when this machine is merely AHEAD of auth, which they describe as "a safe no-op merge (redundant commit, no data loss)". But a redundant merge commit leaves the machine ahead by one MORE commit — if the next cycle classifies Diverged again, that is one junk commit per cycle, ~2,880 a day on a 30s timer. Asked for a definitive trace of whether it self-limits and exactly what stops it, or whether it is Critical.
PLAN COVERAGE GAP (second instance of the same shape): Task 15 wires classify/cycle/ApplyMerge but NOT the background scheduling loop that calls cycle() on a timer. The spec says pull triggers on startup, focus and a 30s timer, reusing the existing AWS sync_thread loop — but no task in the plan does that wiring.
Ruling: fold the scheduling loop into Task 17, which already owns child_sync.rs lifecycle concerns (crash recovery, bounded push retry), and keep the first-run/startup wiring in Task 19 as previously ruled. Recording both rather than discovering at the final review that the engine exists but nothing ever calls it. Cost if wrong: Task 17 grows; the pieces are otherwise complete but inert.
Task 15: review verdict (opus) — spec ✅, thread ownership ✅ clean (no fs::write, index op or commit on the background thread; no mutex added), provenance ✅ (classify guarantees ours_oid != auth_oid so the debug_assert cannot fire), goals.csv ✅ with the "warn and carry ours" judgment endorsed. 2 CRITICAL + 2 Important + 1 Minor.
  CRITICAL 1: the ahead-of-auth loop is NOT self-limiting, and the implementer's premise was false. lgs reconcile mirrors refs/lgs-auth/heads/<branch> UNCONDITIONALLY every tick (engine.rs:458), not only on divergence — so auth is just the last PUBLISHED tip and ANY ordinary local commit leaves us ahead. Ahead classifies as Diverged, produces an empty merge commit, leaves HEAD ahead by one more, and repeats. The only brake is lgs republishing. While a push fails or the daemon is down: one empty commit per cycle, unbounded — thousands a day into the user's allowance history. My Priority-4 suspicion was right and the real case is far more common than "rare".
  CRITICAL 2: HEAD race in apply_merge causing SILENT TRANSACTION LOSS. apply_merge never checks HEAD == parents.0. The UI thread writes and commits transactions; a commit landing between cycle() and apply_merge is ORPHANED — the merged CSV overwrites its content and commit_merge moves HEAD to a commit that does not have it as a parent. The user's transaction disappears with no error. Exactly the failure class this whole project exists to eliminate, arriving through a thread-timing door rather than a merge-logic one.
  Important: apply_merge has ZERO tests — the entire UI half is unexercised. Important: failures are log-only, so a desktop user sees nothing when a merge fails, including the goals.csv divergence warning. Minor: copy_from_slice panics on a SHA-256 repo.
Task 15: fix round 1/5 dispatched — add Cycle::Ahead (merge_base == auth) so "ahead" is not misread as divergence; guard apply_merge on HEAD == parents.0 and discard a merge computed against a moved HEAD; test the UI half including the interloping-commit race; surface failures and the goals warning through SyncMessage/SyncStatus rather than the log.
Task 15: fix round 1/5 (2 Critical + 2 Important + 1 Minor; commits 9782263..32006b5). Both Criticals verified BY MECHANISM, not report:
  Cycle::Ahead added with the arm ordered after FastForward, so Diverged is now reached only when neither side is an ancestor. Loop closure traced definitively: a merge commit has auth as a parent, so merge_base(merge, auth) == auth -> Ahead -> push only; push success republishes and yields UpToDate; push failure propagates Err and the next cycle retries Ahead. No empty-commit loop remains by any route, and the Ahead path does no fs::write, no index op, no commit. The reviewer confirmed the underlying premise directly at lgs engine.rs:457-458 — update_ref(auth_head_ref(b), ..) is unconditional per branch.
  HEAD guard MUTATION-TESTED: disabling it makes refuses_a_merge_when_head_moved_since_it_was_computed fail, and with the guard off the CSV is clobbered before commit_merge even errors — so the test's assertion genuinely bites rather than passing on the return value alone. The interloper is committed through the real transaction_service path and the test asserts it SURVIVES.
  Background-thread push judged correct: Remote::push touches remote refs/objects only, never the working tree, consistent with fetch_lgs already living there.
Task 15: fix round 2/5 dispatched. Three remaining Importants share ONE root cause — events are being reported through a STATE channel. SyncStatus is last-writer-wins, so the goals notice and the stale-head refusal both CLOBBER HasConflicts(n) and are themselves erased by the next StatusChanged. A status describes what the system IS; a notice describes what HAPPENED.
  StaleHead currently signals nothing at all — handle_sync_messages sets local_state_dirty only on Applied, so a merge refused for a moved HEAD is silently dropped until the next timer tick. Sending SyncCommand::PollNow makes the re-run real.
  StaleHead must NOT be an Error: it is the guard working as designed, expected and self-healing. Reporting a correctly functioning safety check as a fault teaches the first consumer exactly the wrong thing.
  Goals divergence becomes its own SyncMessage::GoalsDiverged so the UI can latch it, rather than a status that is guaranteed to be erased — for a message telling the user their goals did not merge, transience is close to useless.
  Post-merge push failure routed through the existing PushFailed shape.
Explicitly NOT building a renderer — that is a later task. The point is that the SHAPE is right before anything consumes it, because the first consumer inherits whatever distinctions exist.
Task 15: fix round 2/5 (3 Important addressed; commits 32006b5..3ce8634). Verified by mechanism: PollNow test asserts an actually-received command off a real mpsc channel (not merely that the arm was reached); the stale-head branch no longer assigns status at all and the test proves it stays Idle; goals_diverged is written ONLY by record_goals_diverged, so the notice survives until explicitly replaced — durable as intended; all ten `self.sync.status =` sites grepped and confirmed to be genuine states, none notice-shaped.
Task 15: fix round 3/5 dispatched for a gap MY OWN round-2 fix introduced. The StaleHead -> PollNow path has no debounce or cap: sync_thread.rs breaks its sleep immediately on PollNow and runs a full fetch/classify/push, so if HEAD keeps moving faster than one cycle round-trip the background and UI threads ping-pong many times a second while the user is simply typing transactions. Safe (StaleHead never writes) and self-terminating once writes settle, but it surfaces as a hot machine and needless lgs-daemon load rather than as a test failure. Fixing with a minimal debounce — 500ms minimum interval, a 5-refusal cap after which the ordinary 30s timer takes over, both reset on a successful Applied — rather than a redesign.
Task 15: also closing a Minor — record_push_failed's `event_id` parameter now carries a merge-commit oid in the git case. Harmless today (log-only) but actively misleading, since the next person to key off event_id's format will be wrong. Renaming to a neutral name with a doc comment naming the shapes it can hold.
Task 15: fix round 3/5 (1 Important + 1 Minor; commits 3ce8634..04eb78b). Verified myself by reading the diff: note_stale_head_refusal returns a four-way StaleHeadPollAction (Send / Debounced / LimitReached / Suppressed), so the limit is logged ONCE per streak rather than on every refusal; note_applied() resets the streak so a later bout starts clean rather than staying suppressed forever; record_push_failed's parameter renamed to `reference` with a doc comment naming both shapes it can hold and stating explicitly why it is not called event_id. 366 tests, 0 failed.
Task 15: complete (commits b6ce84a..04eb78b, review clean after 3 fix rounds)
  Task 15 is the second-most valuable task of the run after Task 7. Two Criticals that both tests and reasoning had missed: an unbounded empty-commit loop that fires on ORDINARY use (any local commit leaves the machine ahead of the auth mirror, which was misclassified as divergence), and a HEAD race that silently orphaned a user's transaction when a write landed between computing and applying a merge. Both were found by an Opus review reasoning from lgs's source rather than from the brief.
CARRIED TO TASK 17: the stale-head debounce/cap state is GLOBAL, not per-child. That is correct for today (no multi-child scheduling loop exists) and was my explicit instruction — minimal debounce, not a redesign. But Task 17 adds the scheduling loop, and at that point one noisy child's refusals could suppress a re-poll a different child needed. Task 17 must either key the debounce by child_id or record why global is still acceptable.
CARRIED TO TASK 17 (restated): Task 17 owns wiring the background scheduling loop that calls cycle() and sends ApplyMerge on startup/focus/30s — the engine is complete but inert until then.
Task 16: dispatched on sonnet with an explicit warning that the plan's brief PREDATES Task 4 and describes since-rewritten code. Base 04eb78b, returned DONE at db6c3ba. Workspace green, 460 tests.
Task 16: the stale-brief warning paid off — the implementer found that the brief's "merge path's BalanceService" does not exist at all post-Task-4, since apply_merge recomputes via the pure allowance_core::balance functions which carry no notifier. Rather than apply a fix to a phantom code path, it moved both fixes to the real live bug site (ApplyRemoteEntity's Transaction arm) and added notifier-safe recalculation there. Reviewer read apply_merge directly and confirmed the claim, judged the addition correct, necessary and in scope, and specifically ruled out my stated risk — it cannot re-trigger the removed commit because update_transaction_balances already uses write_transactions_internal.
Task 16: both transaction fixes verified by ACTUAL REPRODUCTION — the reviewer reverted each fix in turn and observed the corresponding test fail (HEAD mismatch; notify assertion). Tests compare real state via Repository::open head() before/after and rx.try_recv() on a real channel, not that a function returned.
Task 16: CRITICAL — the implementer's honestly disclosed gap is real and leaves the task half-done. Goal and Child apply paths STILL double-commit: goal_repository.rs:111 and child_repository.rs:123 both call commit_file_change unconditionally, so one MCP write still becomes a separate commit on every machine for those two entity types. Same divergence-as-steady-state bug, narrowed rather than fixed.
Task 16: fix round 1/5 dispatched — give goals and children the same non-committing write path transactions now have, route only the sync-apply callers through it, and mirror the HEAD-before/after tests for both. Noted specifically that for child.yaml the write must still land correctly on disk; only the commit is suppressed.
Task 16: fix round 1/5 delivered at 523c860 — Goal and Child apply paths now mirror the transaction fix. 4/4 coexist tests, each verified to fail (HEAD mismatch) when its fix is reverted. 370 passed in the egui lib, 0 failures workspace-wide.
Task 16: implementer flagged a THIRD instance of the same defect — DeleteLocalEntity was never audited. I verified it: delete_transaction at transaction_repository.rs:365 calls write_transactions, the COMMITTING variant, so a remote delete produces a commit on every machine exactly as applies did. delete_transactions (bulk, :372) has the same shape.
Task 16: fix round 2/5 dispatched — route sync-driven deletes for all three entity types through the non-committing writes, leaving user-initiated deletes on the committing path. Required deliverable: a DEFINITIVE statement of whether ANY path reachable from either ApplyRemoteEntity or DeleteLocalEntity still commits, for any entity type. Asking the question once rather than discovering a fourth instance later.
Note on why this kept recurring: the original brief named ONE call site (ApplyRemoteEntity's transaction arm). The actual defect is a PROPERTY of the repositories — every write path committed unconditionally — so fixing it call-site by call-site was always going to surface instance after instance. The right framing was "which repository writes commit, and which callers must not", and the implementer's honest disclosures are what converted a narrow brief into the full sweep.
Task 16: fix round 2/5 delivered at 18e36e6 — delete paths for all three entity types now route through non-committing variants. 7/7 coexist tests, 373 passed in the egui lib, 0 failures workspace-wide.
Task 16: re-review REDID the audit independently rather than verifying the claim, and agrees. Five commit_file_change sites found (transaction_repository:201, child_repository:107, goal_repository:100, allowance_repository:106, parental_control_repository:163) plus commit_merge in git/mod.rs. Transaction/Goal/Child sites are reached only via committing write_*/store_*/delete_* methods, none of which apply_remote_entity or delete_local_entity call — both use exclusively the _no_commit variants. AllowanceRepository is reachable only from AllowanceService (periodic issuance), ParentalControlRepository only from ParentalControlService (login logging); neither appears in the sync arms. sync_manager.rs, sync_thread.rs and child_sync.rs make NO repository or service calls at all, independently confirming the "UI thread owns all repo I/O" invariant. EntityType has exactly Transaction/Goal/Child.
Task 16: deletes verified to GENUINELY DELETE, not merely to skip the commit — retain() filters the row and the reduced set is written via the internal variant; the transaction test asserts the list is empty and the goal test asserts goals.csv no longer contains the description ON DISK, not just return values. The child-delete test that "passes unmodified" was verified non-vacuous — it exercises delete_local_entity end to end (HEAD unchanged, folder survives, registry entry removed).
Task 16: bulk delete_transactions correctly left committing — its only caller is a UI button in header.rs; delete_local_entity calls the singular delete_transaction_by_id only.
Task 16: reviewer reverted the goal fix LIVE and confirmed the corresponding test fails with a genuine HEAD-mismatch panic, then restored.
Task 16: complete (commits 04eb78b..18e36e6, review clean after 2 fix rounds)
Task 17: dispatched on sonnet with Parts A (brief), B (scheduling loop — my addition) and C (per-child debounce, carried from Task 15). Base 18e36e6, returned DONE at 0434295 across three commits. 384 tests, 0 failures. Debounce decision: keyed by child_id via per-child HashMaps, pinned by two isolation tests — the right call, since a global counter would let one noisy child's capped streak suppress a different child's legitimate re-poll once the loop iterates.
Task 17: review verdict (opus) — spec ✅, thread-ownership invariant HELD (no working-tree write, checkout or commit on the background thread), push retry bounded, failure isolation genuine (bogus child fails in child_dir, ordered first, real child's ApplyMerge still asserted to arrive). 2 Critical + 1 Important + minors.
  CRITICAL 1: FastForward is DROPPED — its only consumer is a log::warn at sync_thread.rs:353; SyncMessage has no fast-forward variant, no arm, no checkout. FastForward is the ORDINARY case (peer edited, we did not), so peer commits land in .git and are never checked out and the feature does nothing in its most common situation. My priority-1 suspicion confirmed. Not user-visible today only because production passes child_sync: None.
  CRITICAL 2: the tick is UNBOUNDED. LgsClient::run is Command::output() with no timeout, fetch_lgs/push_lgs pass no timeout callbacks, and run_child_sync_cycles has no time budget and no shutdown check — one hung daemon stalls the AWS poll, the 30s timer AND shutdown indefinitely. Also shells out `lgs status --json` once per child per tick.
  IMPORTANT 3: recover_if_dirty hard-resets on ANY tracked-file dirtiness with no mid-merge check — and this design's OWN AWS path deliberately writes transactions.csv WITHOUT committing, so a dirty tree is a normal steady state rather than a crash signature. Uncommitted remote rows would be reset away. The recovery mechanism destroying user data is worse than the crash it recovers from — exactly the risk I flagged in the dispatch.
Task 17: fix round 1/5 dispatched — add SyncMessage::ApplyFastForward with a UI-thread checkout and a test asserting the file content actually changed on disk; bound the tick (timeout on LgsClient::run, per-tick budget, shutdown check between children, one lgs status per tick rather than per child); replace the inferred recovery trigger with an EXPLICIT marker file written before the merge's first working-tree write and removed after commit_merge succeeds, so recovery fires only on an unambiguous mid-merge crash and leaves an ordinary dirty tree alone.
Task 17: fix round 1/5 (2 Critical + 1 Important + minors; commits 0434295..4e2fcf8). All verified by mechanism: ApplyFastForward checks out on the UI thread with a test asserting the peer's BYTES on disk and HEAD movement; the checkout is NON-FORCE (GIT_CHECKOUT_SAFE default, .force() never called) with a test proving uncommitted bytes survive; checkout-before-ref-move confirmed the crash-safe order and idempotent on re-run (libgit2 treats "workdir already equals target" as unmodified); the timeout drains stdout/stderr on threads started BEFORE the wait loop so it cannot deadlock, and the kill is proven by an elapsed bound that would blow if kill failed; the marker is written before the first working-tree write, cleared only after commit_merge, lives inside .git/ so it is uncommittable and invisible to git status, and an ordinary dirty tree is proven untouched.
Task 17: fix round 2/5 dispatched for two residuals.
  NEW Important, introduced by the FF fix: fast-forward LIVELOCKS in this design's own steady state. The AWS path leaves transactions.csv dirty BY DESIGN (Task 16's deliberate fix), so in the FF case there is no local commit, classify returns FastForward forever, the safe checkout refuses forever, and apply_fast_forward only log::warn!s — neither a status nor a recorded refusal. A machine receiving only MCP writes stops ingesting peer data INDEFINITELY with nothing user-visible. The refusal is correct; the missing fallback is not. Fix: commit the dirty tree (those AWS rows are legitimate data), which turns it into a real local commit so the next cycle classifies Diverged and the merge handles it — including deduping identical rows via intrinsic_eq. Explicitly NOT a reintroduction of what Task 16 fixed: that was about each machine routinely committing the same MCP write; this is a bounded resolution of a blocked fast-forward.
  Important: the hung-fetch stall is real and the per-tick budget does NOT contain it — the budget only gates STARTING a child, so a wedged fetch blocks inside cycle_with_status and takes the AWS poll, the timer and shutdown with it. Fixing properly via git2 RemoteCallbacks::transfer_progress returning false past a deadline, which is genuine transport cancellation, rather than by relocating the call.
  Minor: every checkout_tree error is reported as WouldOverwriteLocalChanges, so an I/O error, corrupt object or permissions problem is misclassified as a benign retry and never reaches SyncStatus.
Task 17: fix round 2/5 (2 Important + 1 Minor; commits 4e2fcf8..c979507). FF livelock ADDRESSED and recurrence DEFINITIVELY ruled out: after the unblock commit merge_base(new_head, to) == old_head != new_head, so every subsequent tick classifies Diverged until a merge resolves it — one commit per blockage, and a blockage needs both a fresh peer advance AND a dirty tree. Not the Ahead-case unbounded shape. Working tree provably intact (byte-identical assertion plus parent check); a stale in-flight ApplyFastForward is caught by the graph_descendant_of guard.
Task 17: the reviewer found something that cuts both ways — SyncStatus is clobbered within a single drain pass (sync_thread sends StatusChanged(Idle) after the apply message; handle_sync_messages drains the whole queue with an unconditional overwrite). That VALIDATES the implementer's choice of a durable FastForwardBlockedNotice over a status, and simultaneously proves that the SyncStatus::Error written by the Minor-3 checkout-error fix is invisible. Correctly classifying an error and then erasing it is the same false-confidence pattern in a new place.
Task 17: fix round 3/5 dispatched:
  Important — add_all(["*"]) stages UNTRACKED STRAYS into the unblock commit, which then gets PUSHED. In a child's data directory that means .DS_Store and editor temp files committed into the child's synced history and propagated to the other machine, permanently. Staging only the four files this system owns instead.
  Important — genuine (non-conflict) checkout failures need a durable notice in the FastForwardBlockedNotice shape, since the SyncStatus::Error they currently write is erased before anything can act on it.
  Minor — GitManager::commit needs an empty-commit guard so a failed add_all cannot produce a content-free commit per peer advance.
Ruling: DEFERRED the connect/ref-advertisement hang rather than forcing a fourth round. transfer_progress fires only once objects arrive and push_negotiation only after advertisement, so a daemon wedged during connect is unbounded on both paths and the push-side deadline is effectively decorative. Bounding it needs a killable worker thread or a socket-level timeout — a different mechanism, larger than this round should carry. Instructed the implementer to record it plainly as a known open gap so it reaches the final review. Cost if wrong: a wedged daemon during connect can still stall the sync thread and delay shutdown; the lgs subprocess path is already bounded, so this is the git2 transport path only.
Task 17: fix round 3/5 (2 Important + 1 Minor; commits c979507..b8372a8). The implementer committed and was then cut off by a rate limit before reporting; the commit message itself documents all three fixes plus the deferral. All verified: FILES_THIS_APP_OWNS replaces add_all(["*"]) with a per-file add that skips non-existent files, and the stray test genuinely creates .DS_Store and asserts tree.get_path(".DS_Store").is_err() on the resulting commit's actual TREE while confirming legitimate content still landed. SyncFailureNotice mirrors FastForwardBlockedNotice, recorded on the non-Conflict checkout branch and all three unblock-commit failure branches, cleared only on a successful apply, never touched by the status path. commit_if_changed compares staged tree oid to parent.tree_id() correctly and returns Ok(None); commit() verified byte-for-byte unchanged so existing callers keep their contract.
Task 17: owned-files list verified COMPLETE against what the codebase actually writes into a child directory (connection.rs, child_repository.rs, allowance_repository.rs, goal_repository.rs confirm exactly those four plus parental_control_attempts.csv).
Task 17: deferred minor — the parental_control_attempts.csv exclusion is sound for the routine case (its writes commit immediately via commit_file_change, so it is never routinely dirty, unlike transactions.csv under the AWS transport). But in a narrow crash-between-write-and-commit window, if it is the ONLY dirty file, the unblock commit legitimately produces nothing and the fast-forward then needs manual resolution rather than self-healing. The commit message's reasoning ("the subsequent merge captures it") only holds if some other owned file also blocks the checkout. Not a routine livelock.
Task 17: deferred minor — a conflict confined to a file the app does not own recurs every tick (same target, same Ok(None), same Failed). Harmless: no commit is ever fabricated, record_sync_failure replaces rather than accumulates, and it costs one cheap checkout attempt per 30s tick rather than a spin. Pinned by a test proving HEAD does not move and the failure is durably surfaced.
Task 17: deferred minor — the pre-existing "cannot determine branch" / "cannot update HEAD" branches still write only sync.status and remain clobbered the same way. Outside what the finding named; not new breakage.
Task 17: complete (commits 18e36e6..b8372a8, review clean after 3 fix rounds, 4 minors deferred)
  Task 17 produced four Criticals across its rounds — the most of any task. It is also where every earlier piece finally interacts, which is exactly where the seams show. Two of the four came from the interaction of two individually CORRECT decisions: Task 16 stopping the AWS path from committing, and Task 17 using a safe non-force checkout. Each was right; together they wedged the ordinary fast-forward path permanently.
Task 18: dispatched on sonnet with ABSOLUTE safety constraints (never run the migration against the real data, not even to verify; the live iCloud child folder and children.yaml are read-only). Base b8372a8, returned DONE at 5c3d73b. 8 migration tests, workspace green, 414 in the egui lib. Implementer confirmed the real data was never touched and used a per-test fake lgs binary.
Task 18: review verdict (opus) — spec ✅. SAFETY SHAPE CONFIRMED CORRECT by reading every function: zero remove_file/remove_dir/rename/truncating-open in the module; old_dir appears only as a copy/read SOURCE; RepointRegistry is genuinely last with only in-memory pushes after it; ChildRegistry::save is temp-file-plus-rename (atomic). The legacy-precision count reaches a Warning StartupNotice naming the old folder, pinned by a test. The fake lgs binary cannot be mistaken for the real one (per-test TempDir, reached only via an explicit lgs_binary path, never on PATH). 2 CRITICAL + 3 Important + 5 minors.
  CRITICAL 1: THE ADOPT BRANCH DESTROYS THE FIRST MACHINE'S WORK. RestoreExisting clones A's history, then CopyDataFiles/Canonicalize overwrite the restored files with B's STALE iCloud copy and commit on top. Ancestry is preserved so the deletion FAST-FORWARDS AND WINS — every row A wrote after migrating is silently removed. No union merge exists anywhere on that path. This is exactly the data loss adoption was added to prevent, inverted. The implementer disclosed the branch was untested end-to-end; the reviewer judged the severity understated, and inspection is what surfaced it.
  CRITICAL 2: the plan is NOT RE-RUNNABLE. plan_lgs_migration consults only status.adoptable, never status.projects, so a retry after a failure at or after LgsAdd takes the InitAndPush branch and calls `lgs add` on an already-registered project — permanent failure, no resume path. A migration that cannot be retried after a partial failure strands the user.
  Important: is_cloud_synced's doc says canonicalizing is the CALLER's job and the only production caller passes a lexical path, leaving the symlink hole open.
  Important: the failure tests assert only old_child_dir().exists(), so a bug truncating old/transactions.csv would pass all three. Existence is not content.
  Important: the duplicated owned-files list — and the report's justification was WRONG. FILES_THIS_APP_OWNS and DATA_FILES are in the SAME crate (egui-frontend/src/lib.rs:16 pulls backend/mod.rs via #[path]); there is no crate boundary. Fourth duplicate-definition instance in this project.
  Minor worth noting: run_commit uses add_all — the exact thing Task 17 removed one task earlier for sweeping untracked strays into a pushed commit.
Task 18: fix round 1/5 dispatched — reconcile adopt via allowance_core::merge with an empty base (the union is correct: with no common ancestor nothing can be shown deleted) plus a real bare-repo-and-clone fixture proving A's post-migration row SURVIVES; make the plan resumable from status.projects; canonicalize before the guard; assert file CONTENTS in the failure tests; single-source the owned-files list in paths.rs; and all five minors.
Task 18: fix round 1/5 (2 Critical + 3 Important + 5 minors; commits 5c3d73b..6cb577f). 12 migration tests, workspace green, 418 in the egui lib.
  Critical 1 ADDRESSED and verified end to end: run_canonicalize now parses both sides and calls merge(None, ours, theirs), writing the union; run_copy_data_files never overwrites an existing dst, byte-compares instead and surfaces discrepancy names as a StartupNotice with the RESTORED version winning. The test inits a REAL bare repo, seeds tx-a1 then tx-a2 ("A's row after migrating"), pushes, drives the whole plan with a fake `lgs restore` that performs a real `git clone`, and asserts tx-a1, tx-a2 AND tx-b1 all survive plus that B's stale goals.csv did not win.
  Critical 2 ADDRESSED: already_registered reads status.project(); LgsAdd is pushed only when neither adopting nor already registered. Resume keys off DURABLE state only — status.projects, dst.exists(), idempotent init_repo, commit_if_changed, self-healing ensure_lgs_remote — with three resume tests each running a genuinely failing attempt first.
  Important 3-5 and all five minors ADDRESSED, including add_all removed from run_commit (the Task 17 regression) and ONE pub(crate) FILES_THIS_APP_OWNS in paths.rs with DATA_FILES derived by filtering rather than restated — the fourth duplicate-definition trap closed.
  Synthetic provenances confirmed DISTINCT in both fields ({0,[0;20]} vs {-1,[1;20]}), so no debug_assert panic (dev profile keeps debug-assertions despite opt-level=1), and the claim traced: with base None, base_known is false so wins() is reachable only from the add/add arm, which keeps BOTH rows and merely chooses which keeps the plain id versus the content-hash suffix.
Task 18: fix round 2/5 dispatched for two minors — the adopt test asserts the WORKING-TREE file rather than HEAD:transactions.csv, so a bug staging nothing would leave Push a successful no-op and the assertion guarding the data-loss Critical would still pass; and snapshot_old_dir unwraps fs::read per entry so it would panic on a real old folder containing .git/, which the live folder does.
HANDOFF TO TASK 19: only the guard's CANDIDATE is canonicalized — env.home and cloud_root are not, so a symlinked home would make the MobileDocuments and DocumentsSyncOn rules fail OPEN. The tell is that Task 18's test has to canonicalize cloud_root itself. No production SyncPaths constructor exists yet and Task 19 owns building one, so canonicalizing both sides belongs there.
Task 18: fix round 2/5 (2 minors; commits 6cb577f..6bf4efa). Verified myself by reading the diff — and the implementer went further than asked: the adopt test now asserts BOTH the committed tree (HEAD:transactions.csv via read_committed_file) AND the bare repo's own branch tip (read_pushed_file), proving the rows actually REACHED THE REMOTE rather than merely landing in a working tree. "Pushed" is the property that makes A's rows durable on the other machine, which is the whole point of the test. Same treatment applied to the goals.csv assertion. snapshot_old_dir now filters to plain files with a comment noting the REAL folder contains a .git, so the helper cannot blow up on a realistic fixture.
Task 18: complete (commits b8372a8..6bf4efa, review clean after 2 fix rounds)
  Task 18's Critical was the single most dangerous defect of the run: the adopt branch cloned machine A's history and then committed machine B's STALE copy on top, and because ancestry was preserved the deletion fast-forwarded and won — silently erasing every row A wrote after migrating. It was found by INSPECTION, not by testing, on a branch the implementer had honestly disclosed as untested end-to-end while judging it "only matters on a second machine". The reviewer's call that the severity was understated is the reason it did not ship.
Task 19: dispatched on sonnet with Part A (brief onboarding), Part B (first-run/startup wiring — my addition, since app_state.rs passed child_sync: None and made every piece of Tasks 11-18 dead code), and Task 18's canonicalization handoff. Base 6bf4efa, returned DONE at f938ef1. 437 tests in the egui crate, 0 failed; 9/9 onboarding tests. Sync engine now reachable after a first-run that persists a cloud_root, effective on the next app start.
Task 19: review verdict (opus) — spec ✅. Confirmed GOOD on the three things I flagged: NullRemoteStorage does NOT harm AWS users (constructed only in the else of will_spawn_aws; no watermark advance, no checkpoint write) and does NOT error-loop for lgs-only users (all methods Ok, zero events, watermarks preserved, only a Syncing->Idle toggle per tick); the old neither-transport-means-no-thread behaviour is preserved; the guard now FAILS CLOSED with both home and cloud_root canonicalized and a canonicalization failure returning Err; and install ownership is recorded AFTER a successful install, with a crash-before-persist degrading to ReportSkew — the safe direction. 1 CRITICAL + 5 Important + minors.
  CRITICAL: FIRST RUN IS SILENTLY ERASED WITHIN 30 SECONDS ON THE USER'S ACTUAL MACHINE. sync_loop rewrites the WHOLE sync_state.yaml every iteration from its SPAWN-TIME snapshot (cloud_root: None), while start_lgs_first_run writes cloud_root: Some(..) into the same file from the UI thread. The user's sync_state.yaml has enabled: true and a valid AWS URL, so that thread is already running — their first run is clobbered before they can restart, the feature never activates, and nothing reports an error. daemon_ownership: true is lost identically.
  THIS IS THE RETURN OF A BUG WE ALREADY FIXED. Task 12's implementer found and fixed exactly this for daemon_ownership; Task 12's review then called the pattern "self-alerting but not self-protecting" (adding a field forces E0063 at every literal, but the error can be resolved correctly OR with Default::default(), silently reintroducing it) and recommended mutate-in-place. I DEFERRED that recommendation as a minor. It has now bitten, precisely as predicted, on a field added two tasks later. Fixing structurally this time — re-read before save and merge, or persist watermarks separately — so no future field can be clobbered by the same mechanism.
  Important: an ARCHIVED adopted child becomes a permanent failure loop (archived consulted nowhere in child_sync; push refused 403 every 30s, SyncStatus::Error). Important: AdoptableEntry drops lgs's archive_unreadable, so "could not read the archive record" renders as "not archived" — exactly what lgs's own comment warns against, and the same shape as is_confirmed_backed_up returning false for Unknown. Important: an invalid remote_url with lgs configured silently substitutes the null and makes the pre-existing URL warning unreachable, so AWS stops syncing with no diagnostic. Important: bootstrap runs on the main thread before the first frame with unbounded launchctl/id calls, so a wedged launchctl hangs the app on launch. Important: sync_loop drains retry_queue.yaml through push_event unconditionally, so with the null remote a leftover queue is silently "pushed" and DISCARDED — real queued AWS events lost with no trace.
Task 19: fix round 1/5 dispatched with all six.
Task 19: fix round 1/5 (1 Critical + 5 Important; commits f938ef1..4299868). 451 tests in the egui crate, +14 new.
  CRITICAL fixed STRUCTURALLY, which was the whole question: persist_watermarks re-reads sync_state.yaml and assigns ONLY .watermarks, never enumerating other fields, so a field added tomorrow survives with nobody needing to remember to thread it. Exactly one non-test save site in sync_thread.rs, confirmed by grep. This closes the CLASS that Task 12 only closed for one instance — the deferral I made there is what allowed the second occurrence, and the recommendation I deferred (mutate-in-place) is essentially what got implemented now.
  Test caveat recorded: the survival test calls persist_watermarks directly rather than through a real loop save. It would still catch a regression to the old approach (the fallback has cloud_root: None), and the loop provably has one save site, so inspection covers the gap.
  Important 1-5 all addressed: archived children skip the push (Cycle::Ahead if archived -> ArchivedSkipped, with a test asserting the bare's refs/heads/main never moved and a non-archived twin proving the assertion discriminates); archive_unreadable carried and labelled unknown rather than safe; the AWS URL diagnostic keys off will_spawn_aws before the branch; launchctl calls genuinely bounded with kill-on-expiry and drained pipes; the retry queue RETAINED because null writes now Err so the event returns to remaining_retries and is re-saved — nothing lost on a later real remote.
Task 19: fix round 2/5 dispatched — the SAME clobber in the OPPOSITE direction (lgs_sync_modal.rs:290 loads SyncState, blocks up to ~50s through first-run and daemon setup, then saves that stale snapshot at :336, erasing any watermark the loop wrote in the window — one-line fix, move the load next to the save), plus making SyncState::save atomic via temp-file-plus-rename, mirroring ChildRegistry::save which already does. A crash mid-write currently leaves a truncated sync_state.yaml that the loop re-reads every 30 seconds.
FINISH-LINE ITEM FOR THE USER (not fixable inside a task — no task owns the UI): SyncUiState::sync_failures, goals_diverged and fast_forward_blocked are ALL STORED AND NEVER RENDERED, read nowhere outside their own record/clear helpers. Every durable notice this project added is invisible to the user. So a failed goals merge, a blocked fast-forward and a genuine sync failure are all recorded correctly and shown nowhere. Two of the three are pre-existing; none has a consumer.
Task 19: accuracy correction recorded — the archived post-merge push residual is THREE attempts (push_with_retry(.., 3)), not one, and the failure reaches record_push_failed which is log-only, so no error surfaces and clear_sync_failure transiently wipes the archived notice. Bounded per divergence, next tick skips.
Task 19: fix round 2/5 (2 Important; commits 4299868..78f06fe). Verified myself by reading the diff — the modal now reads ONLY daemon_ownership up front (to decide ensure_daemon's action), then re-reads immediately before the save and sets only cloud_root and daemon_ownership, so whatever the background thread wrote to watermarks during the ~50s blocking window survives. The comment explains the reasoning at both sites. SyncState::save made atomic with 3 new tests.
Task 19: complete (commits 6bf4efa..78f06fe, review clean after 2 fix rounds)
  Task 19's Critical mattered more than most because it was invisible AND specific to the user's machine: first-run's cloud_root was erased within 30s precisely BECAUSE they have AWS enabled, so the clobbering thread was already running. The feature would have silently never activated, with no error anywhere. And it was the SECOND occurrence of a defect Task 12 fixed for one field — the mutate-in-place recommendation I deferred there is essentially what shipped here.

=== ALL 19 IMPLEMENTATION TASKS THROUGH TASK 19 COMPLETE ===
Remaining: Task 20 (Check sync action), Task 21 (manual acceptance checklist), then the final whole-branch review.
Task 20: dispatched on sonnet, base 78f06fe, returned DONE at 5483bbe. All eight stages, 462 tests in the egui lib, 0 failures. Local bare repos and a fake lgs reached only by explicit path.
Task 20: review verdict — spec ✅, implementation solid. Confirmed good: stages named and ordered, stop-at-first-failure genuine, .sync-check outside FILES_THIS_APP_OWNS and staged via add_file not add_all, LgsClient::sync documented ack-only with the Fetch loop never trusting it, no panics outside tests. Folding cleanup failure into the failing stage's detail was judged CORRECT rather than a shortcut, because appending a later stage would violate the brief's own `r.stage <= failing_stage` contract. 2 Important + 1 Minor.
  Important: EVERY "Check sync" click leaves two permanent commits in the family's real financial history. The add commit is pushed, then a remove commit — but a removal does not erase the add, which stays in the remote DAG forever and propagates to the other machine. Troubleshooting means clicking repeatedly, so the diagnostic writes ever-growing noise into the thing it diagnoses. And if the recovery push fails, the remote keeps an un-removed sentinel needing manual attention. The reviewer judged this "not acceptable as a long-term design" but matching the brief as written, and recommended deferring. THE BRIEF IS MINE, so I am fixing it rather than deferring.
  Important: the Fetch poll's true worst case is ~40s, not the documented 30s, because each iteration calls `lgs sync` which can itself consume the full 10s subprocess timeout — and the action runs synchronously on the UI thread, so that is the freeze a non-technical user gets from one button.
Ruling: redesign the probe to never touch the shared branch. Build the sentinel commit with git2's update_ref = None so HEAD and the working tree are untouched, push it to a disposable `refs/sync-check/probe` (not a branch, so lgs's reconcile and the refs/lgs-auth/heads/* refspec both ignore it), fetch it back, then delete the ref both sides. This eliminates BOTH problems at once: no commit ever enters the branch history, and a cleanup failure can only orphan a throwaway ref instead of polluting the user's branch. It also removes the working-tree write entirely, so the disk-cleanup failure paths disappear. And since pushing/fetching our OWN ref is a round trip to the local daemon's bare repo with no reconcile or peer dependency, the poll — and with it the UI freeze — should become unnecessary. Cost if wrong: slightly more git plumbing in the probe; the eight stage names are retained so the brief's sequencing contract still holds.
Task 20: fix round 1/5 (2 Important; commits 5483bbe..a308155). Both ADDRESSED at the mechanism level. The probe commit is now built with repo.commit(None, ..) — update_ref literally None, so HEAD never moves — and recorded only under refs/sync-check/probe via a direct reference() call. check_sync no longer even takes a GitManager; grepping the new code shows ZERO working-tree or index writes. Push/fetch/delete use dedicated refspec helpers on refs/sync-check/* and a distinct local tracking ref, never refs/heads/*. The push is genuinely forced (+refs/sync-check/probe:refs/sync-check/probe), pinned by a non-fast-forward overwrite test, so an orphaned ref self-heals on the next run. The poll is gone entirely: Push and Fetch are single attempts under the honest 30s NETWORK_TIMEOUT rather than an understated bound that could reach 40s. Eight stage names retained with accurate doc comments; the sequencing contract still passes. 465 tests, 0 failures.
Task 20: I closed the reviewer's one residual caveat myself rather than leaving it asserted — lgs's durability engine reads ONLY refs/heads/ (list_heads, engine.rs:64-72) and refs/tags/ (list_tags), so refs/sync-check/* is invisible to it and a stray probe ref CANNOT cause a false diverged or unbacked-up report. That mattered because the alternative would have been the diagnostic tool causing the alarm it exists to investigate.
Task 20: deferred minor — LgsClient::sync is now unused in production (its own test only). Leaving it: it is a single legitimate CLI wrapper, not a dead DUPLICATE of a live path, so it is not the trap that misled three readers earlier in this project.
Task 20: deferred minor — the action still runs synchronously on the UI thread, worst case 2x30s on a wedged daemon. Matches the modal's existing pattern; much improved from ~40s plus a poll.
Task 20: complete (commits 78f06fe..a308155, review clean after 1 fix round)
  Task 20's finding was a design flaw in MY brief rather than an implementation defect: the literal step sequence I specified ("commit, push, fetch, read back, then remove it and commit the removal") guaranteed two permanent commits per click in the family's financial history. The reviewer judged it not acceptable long-term but spec-compliant and recommended deferring; I fixed it instead, because deferring my own error is how it ships.
Task 21: dispatched on sonnet, base a308155, returned DONE at b9b9fff. docs/lgs-sync-acceptance-checklist.md, 8 items.
Ruling: accepted Task 21 without a separate review dispatch. It is a document, I read it in full, and it meets the bar — each item gives what to do, what to observe, what a PASS looks like, and names its known failure mode; it distinguishes confirmation (item 5, cargo-bundle layout) from risk; it states the inherited unproven Proton assumption plainly with the reasoning that neither checklist has verified the ground its cold start stands on; and the "what CI already covers" section tells the reader what NOT to re-test by hand, which matters because manual re-testing here means touching real data and real daemons. Cost if wrong: a checklist item is unclear when someone follows it; the run-history discipline means a confused run gets recorded rather than lost.
Task 21: complete (commit a308155..b9b9fff)

=== ALL 21 TASKS COMPLETE ===
Next: the final whole-branch review on the most capable model, pointed at every deferred minor and parked ruling in this ledger.

=== FINAL WHOLE-BRANCH REVIEW (opus) ===
Verdict: do not ship as-is; three Criticals. Tests at that point: 593 passed, 0 failed, 4 deliberately gated, 23 binaries.
  CRITICAL 1: apply_merge overwrote transactions.csv from the two COMMITTED tips, while Task 16 deliberately made the AWS path non-committing — so a dirty file is the DESIGNED steady state and an MCP-authored uncommitted row was erased by the next merge, unrecoverably (the AWS watermark had already advanced). The HEAD guard caught only moved commits. Task 17 protected the fast-forward path with a safe non-force checkout; the merge path had bare fs::write and no equivalent. The comment above it asserted the opposite. THIS IS THE TASK 16/17 SEAM I TOLD THE REVIEW TO HUNT FOR, found at the only altitude where it is visible.
  CRITICAL 2: commit_merge still used add_all(["*"]) — removed from the unblock commit in Task 17 and from run_commit in Task 18, both for sweeping .DS_Store and editor temps into a pushed commit, but never revisited in the MAIN pushed path.
  CRITICAL 3: migration was DEAD CODE with no production caller, so the machine holding the real data would never migrate, no allowance-* project would exist, and every tick would fail project_from_status. ~1,200 lines of reviewed migration, including Task 18's data-loss fix, unreachable.
  Also corrected MY ledger: the goals.csv "spec gap" was misfiled — the spec never lists goals.csv as a merged row file. The real residual is DomainGoal.target_amount remaining f64.
=== FINAL FIX WAVE (one dispatch, per the workflow) ===
All six items done at 1f2aa4c. Single scoped re-review: ALL SIX ADDRESSED. 597 passed, 0 failed, 4 ignored, 24 suites.
  Critical 1's reschedule proven BOUNDED with a two-cycle trace: the commit CONSUMES the dirtiness (tree matches the new HEAD, so clean), the stale merge is discarded, and cycle 2 recomputes against the new HEAD and applies. Commits are bounded by external AWS write count, not self-sustaining — and it reuses the EXISTING note_stale_head_refusal debounce and 5-cap rather than creating a second unbounded reschedule path. That was the risk the fix itself introduced and it is closed.
  Critical 3 verified non-invasive: migration_lgs.rs is not in the commit at all, so its heavily-reviewed logic is untouched; the caller filters on !path.starts_with(children_root) and is idempotent across restarts.
=== RESIDUALS ADJUDICATED (no second fix wave, per the workflow) ===
Ruling: the four residuals below are NOT fixed and are surfaced to the user instead. The workflow allows exactly one fix wave, and none of these reintroduces data loss on the designed steady-state path. Two deserve follow-up and the user should decide whether to spend another round now.
  Important residual A — the Ok(None) branch (tree dirty, but no OWNED file changed) leaves HEAD and the dirty tree unchanged, so every future merge for that child is discarded FOREVER with no Error and no notice. Reachable via a dirty tracked non-owned file (parental_control_attempts.csv, whose commit_file_change failures are swallowed with `let _`) or a deleted owned file. A silent permanent stall rather than a commit storm. Cost if left: one child stops syncing indefinitely and nothing says so.
  Important residual B — the new guard sits AFTER recover_if_dirty, which still hard-resets when the crash marker is present, so an MCP row sitting in that same dirty tree is still destroyed. Narrow (requires a crash mid-merge) but it is the same data-loss class, and the new comment overclaims by not saying so. Cost if left: a transaction written by the MCP server can be lost if the app crashed mid-merge beforehand.
  Minor residual C — commit_file_change's fallback no longer stages parental_control_attempts.csv, so on that rare path its write is not committed. Deliberate and documented.
  Minor residual D — migration does not refresh the in-process registry, so post-migration same-session writes go to the old folder; mitigated by the "Restart the app" message. Its "Open this dialog again to retry" advice is unreachable once cloud_root is set.
