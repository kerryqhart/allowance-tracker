feat: Migrate to egui 0.31.1 and fix money transaction modal cancel buttons

## Major Framework Upgrade
- Upgraded egui from 0.28 → 0.31.1 (strategic choice to avoid 0.32 alpha blending regression)
- Updated eframe, egui_extras to 0.31.1
- Updated egui_plot to 0.32.1 (compatible with egui 0.31.1)
- Resolved 160+ compilation errors from breaking API changes

## Breaking Changes Resolved
- `egui::Rounding` → `egui::CornerRadius` with u8 values
- `rect_stroke()` now requires `egui::StrokeKind::Outside` parameter
- `egui::FontData` now requires `Arc<FontData>` wrapping
- `egui_plot::Line::new()` and `Points::new()` require name parameter
- Various deprecated method renames (.rounding → .corner_radius, etc.)

## Bug Fixes
- Fixed cancel buttons in money transaction modals not closing overlays
- Added missing `self.calendar.active_overlay = None` in cancel handlers
- Restored original transparency behavior (no visual regressions)

## Technical Benefits
- Avoided egui 0.32 alpha blending regression that made UI elements appear dull
- Maintained modern framework features with stable, recent version (March 2025)
- Clean codebase without brightness compensation workarounds
- Zero performance regressions observed

## Files Changed
- Updated Cargo.toml dependencies
- Fixed API compatibility across 40+ source files
- Updated EGUI_UPGRADE_PLAN.md with complete migration documentation
- Fixed money transaction modal cancel button functionality

All features working correctly with improved framework stability. 