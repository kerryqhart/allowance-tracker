feat: Add profile modal to display child information

- Create ProfileModal component showing child name and formatted birthday
- Wire up "Profile" option in settings menu to open modal
- Add beautiful CSS styling consistent with existing modals
- Format birthdate from YYYY-MM-DD to readable format (e.g., "June 15, 2015")
- Handle case when no active child is selected
- Include smooth animations, backdrop blur, and responsive design

The profile modal provides a clean, read-only view of the active child's
basic information with an intuitive user experience. 